# 작업보드 — 스프린트 1: 실사형 설비 + 실적 분석

상태: `[ ]` 대기 · `[~]` 진행 · `[x]` 완료 · `[?]` 막힘(협업로그 참조)

## 라운드 1 — 최민준 (기반) — 2026-09-07 완료 (handoff-최민준.md)
- [x] `equipments.type` 컬럼 마이그레이션 + 시드 매핑(인터페이스 §1) + API/배치 JSON에 type 반영 — 기존 DB 11대 매핑 확인
- [x] 관리자 콘솔 설비 유형 선택(맵 에디터 패널·설비 마스터)
- [x] 게임 스프라이트 로더(매니페스트 기반, 없으면 절차적 큐브) — 매니페스트 없어도 동작 (이미지 경로는 브라우저 생성 텍스처로 검증)
- [x] 분석 탭 훅: admin.html 탭/컨테이너/스크립트, admin.js에서 `FWAnalytics.mount`, index.js에서 `registerAnalytics` 호출(파일 없으면 건너뜀)
- [x] `public/assets/equipment/` 폴더와 임시 매니페스트(빈 types) 생성
- [x] handoff-최민준.md 작성, 협업로그에 한도윤·서지안에게 시작 신호
- [x] 커밋 (push는 라운드 3)

## 라운드 2 — 한도윤 ∥ 서지안
### 한도윤 — 2026-09-07 완료 (handoff-한도윤.md)
- [x] 설비 유형 7종 Blender 절차 모델 스크립트(`build_equipment.py`) — 프레스, 용접기, 로봇, 조립 라인, 검사기, 포장기, CNC
- [x] 2:1 아이소메트릭 직교 카메라·조명·재질(금속·도장·안전색), 바닥 그림자 — 카메라 X회전 60°(캘리브레이션 실측 64×32), 그림자 알파 28~60
- [x] 배치 렌더(`render.cmd`) → PNG 192×192, `postprocess.py`로 앵커 정렬·매니페스트 생성 — EEVEE 약 15초, 검증 exit code
- [x] 실제 렌더 결과 `public/assets/equipment/*.png` + `manifest.json` 커밋 대상 산출 — 7장 36.4 KB, 육안·알파 통계 확인 (커밋은 라운드 3)
- [x] handoff-한도윤.md: 램프 좌표, 라벨 위치, 최민준에게 요청 사항 — 협업로그에 §2 각도 정정·요청 4건
### 서지안 — 2026-09-07 완료 (handoff-서지안.md)
- [x] `docs/분석-정의.md`: OEE 3요소 정의·계산 근거·데이터 한계(수동 입력 vs 게이트웨이) — 계획 시간·목표 귀속·게이트웨이 지표까지 정의
- [x] `server/analytics.js`: 인터페이스 §4 라우트 3개 구현 (SQLite 쿼리, 기간·설비 필터, 400/401 처리)
- [x] `public/js/analytics.js` + css: OEE 표·막대, 일별 생산 추이, 게이트웨이 연결 품질 — 다크·라이트 확인, 데이터 없음 처리
- [x] 실제 DB로 호출해 값 검증(WLD-01·ASM-01·PRS-01 수기 계산과 대조 일치) → handoff-서지안.md
- [x] (최민준에게 요청) 운영 정책 `shiftMinutesPerDay` 입력란 — 라운드 3에서 반영(입력란 + 서버 검증, 경계값 400 확인)

## 라운드 3 — 최민준 (통합) — 2026-09-08 완료 (handoff-최민준.md)
- [x] 한도윤·서지안 handoff의 요청 반영 — 한도윤 4건(로더·램프·라벨 확인, .gitignore, 그림자 클릭 통과 결정=로더 임계값 64, 각도 인용 없음), 서지안 3건(정책 입력란·검증, README·가이드 링크, 가동률 라벨 구분). 되돌린 산출물 없음
- [x] 게임 화면(다크·라이트·저사양 11대 이미지, 그림자 히트 0/본체 100%)·관리자 📈 탭·유형 select·정책 입력란 실제 확인, `node --check` 21파일, 부하 테스트 50명 30초 통과(이동 p95 21.1ms, 채팅 p95 10.1ms)
- [x] README(스프린트 1 절 완성·구조·파이프라인·검증 수치)·운영 가이드(§3 정책·§3-1 분석·체크리스트)·소개 브리프(§3 실사형 설비·분석·세 에이전트 협업, §4 현재 상태) 갱신
- [x] 커밋·push (GitHub je31061/game_mes main) — 두 사람 산출물 포함, data/·assets/blender/out/ 제외
- [x] handoff-최민준.md 갱신, 스프린트 2 후보 정리

---

# 스프린트 2: BLDC 500W 모터 라인 — 공정(BOP)·단품(BOM)·분해도 (2026-09-09 PM 결정)

사용자 요청: 첨부 자료(BLDC_500W_48V BOM/BOP 마스터 xlsx, 분해도 PDF)를 참조해 해당 공정을 맵으로 표현하고,
**설비를 클릭하면 그 공정에 투입되는 단품이 보이게** 한다. 원천 데이터는 PM이 준비했다:
- `docs/bldc/bldc-500w-48v.json` — 제품 사양, 서브어셈블리 9, 단품 49, 공정 24(아마추어 10 + 조립 14), 공정별 투입 P/N, 분해도 단계 대응(stagePn)
- `public/assets/parts/<PN>.png` — 분해도 서브어셈블리 이미지 9장 (325×484, 흰 카드)
- `docs/bldc/layout-bldc-500w.json` — 2존·설비 24대(코드=공정번호, 유형, op)·라인 23개, `replace: true`
- 계약: 인터페이스 §7(데이터 모델·API), §8(단품 HUD), §9(새 유형 8종)

## 라운드 1 — 세 명 병행 (파일 소유권 준수, 공용 파일은 최민준만)
### 최민준 — 2026-09-09 완료 (handoff-최민준.md)
- [x] 스키마: products/processes/parts/process_inputs, `equipments.op`, `equipments.hidden`·`zones.hidden` 마이그레이션 — 기존 DB 기동 시 자동(멱등), `EQUIPMENT_TYPES` 16종·`inferEquipmentType` OP 표
- [x] `POST /api/admin/bop/import`(JSON upsert), `GET /api/admin/bop`, `GET /api/bop/exploded` — + `POST /api/admin/bop/apply-sample`, 오류 400/401 확인
- [x] 배치 JSON `equipments[].op` + `replace: true`(숨김 처리), hidden 설비 제외(init·world:refresh·분석·게이트웨이), 콘솔 숨김 표시·복원 — `listEquipments/listZones/listLinks`에 `hidden = 0`, 설비 마스터 [복원]/[숨김]·숨긴 존 복원, 왕복 검증
- [x] `equipment:detail`에 `process` 블록, HUD "공정 · 단품" 섹션 + 🔩 분해도 모달 — 다크·라이트, 병목/완성/투입 없음 3종 화면 확인
- [x] §9 새 유형 8종을 `EQUIPMENT_TYPES`·콘솔 라벨에 등록(스프라이트 없으면 큐브) — 한도윤 매니페스트 15종으로 24대 전부 이미지 확인
- [x] 관리자 콘솔에 "제품 라인 적용" 버튼(BOP JSON + 배치 JSON 순서 적용) 또는 맵 에디터 가져오기로 절차 문서화 — 버튼 + BOP JSON 가져오기 + 현황 줄, README·도면-AI-연동에 절차
- [x] 실제 DB에 BLDC 라인 적용 후 게임에서 설비 클릭 → 단품 표시 확인, handoff·협업로그 — 24 표시/11 숨김, OP-A40 단품·분해도, 근접·상태 변경·라인 부하 정상. 커밋(내 파일+PM 원천, push는 라운드 2)
### 한도윤 — 2026-09-09 완료 (handoff-한도윤.md)
- [x] §9 새 유형 8종(stacker, winder, vpi, oven, magnetizer, balancer, dispenser, smt) Blender 모델·렌더 → PNG + 매니페스트 갱신 (§2 규격, 그림자 알파 ≤ 63) — 매니페스트 15종, PNG 합계 79.8 KB, 알파 1~27 픽셀 0·본체 ≥ 96, 다크·라이트 시트 육안 확인. `postprocess.py` 주석 24→64 갱신
- [x] 분해도 PDF 이미지 9장을 게임 HUD 썸네일용으로 정리(필요 시 카드 여백 트리밍·96px 썸네일 `public/assets/parts/thumb/`) — 원본은 유지 — 96×96 불투명·흰 배경·1px 회색 테두리, `make_part_thumbs.py`로 재생성, 원본 무변경
- [x] handoff-한도윤.md(램프 좌표 표), 협업로그 — 15종 램프·labelY·bbox 표, 썸네일 규격, 최민준 요청 4건·서지안 TYPE_LABEL 알림 (커밋은 라운드 2 최민준)
### 서지안 — 2026-09-09 완료 (handoff-서지안.md)
- [x] `docs/분석-정의.md` §8 라인 밸런스 — 유효 C/T·배치 개당 C/T·병목(동률 규칙)·UPH·일 생산량·목표 C/T·밸런스 효율·제품 min(라인)·xlsx 기준값 대조·한계 6가지
- [x] `server/analytics.js` `GET /api/admin/analytics/line-balance` — 쿼리 hours/oee/targetUph/parallel[OP]/hints, 스키마·BOP 없으면 ok:false+reason, 설비 연결(hidden 제외), 400 검증. 기존 3개 라우트 hidden 제외
- [x] `public/js/analytics.js`+css 📈 "라인 밸런스" 카드 — 설정 컨트롤·제품/라인 타일·C/T 막대(병목 강조·배치 빗금·병렬 전 점선·목표 C/T 선·설비 상태 점)·표(병렬 입력)·데이터 없음 3종 안내, 다크·라이트 확인
- [x] 검증: JSON 원천 50항목 + 인메모리 sqlite 라우트 + 실서버 API + 실제 📈 탭 — xlsx 12_Line_Balance 일치(80/1360, 60/1020). handoff·협업로그(최민준 요청 6건) 갱신, 커밋·재기동 안 함

## 라운드 2 — 최민준 (통합) — 2026-09-10 완료 (handoff-최민준.md)
- [x] 한도윤 스프라이트·서지안 라인 밸런스 반영, 실제 화면 확인(다크·라이트), 문법 검사, 부하 테스트 — 규격 검사 통과·되돌림 없음, HUD 썸네일 thumb 우선·원본 폴백, `node --check` 20파일, 24대 이미지·단품 표·분해도 모달, 📈 가로 넘침 수정(admin.html), 부하 50명 30초 이동 p95 18.9ms·채팅 p95 9.4ms
- [x] PM 결정 (3) `FW_SEED_PRODUCT_LINE=bldc` 빈 DB 자동 적용 + `render.yaml` — 격리 서버 3002: 자동 적용 로그, linked 24/24, 표시 24·숨김 10, 재기동 건너뜀
- [x] README·운영 가이드·배포 가이드·소개 브리프 갱신(제품 라인 적용 절차, 단품 HUD, 라인 밸런스, 체험 서버 자동 시작), 커밋·push
- [ ] Render 자동 재배포 확인 (push 후 — handoff "Render 반영 확인")
- [x] handoff·작업보드·협업로그

## 스프린트 3 후보 (handoff-최민준.md "열린 항목 / 스프린트 3 후보")
- [ ] 게이트웨이 폴링 수신/실패 카운트 표 `gateway_samples` → 공정별 실측 C/T (서지안·최민준)
- [ ] 제품 여러 개 `equipments.product_code` (최민준)
- [ ] 분해도 5·6단계(FS-7010·FS-7020) 공정 연결 보완 여부 (PM)
- [ ] 숨김 탭 부팅 시 캔버스 0×0 — `visibilitychange`에 `scale.refresh()` (최민준)

## 스프린트 2 후보 (이전 목록 — 위 BLDC 작업 뒤에 재검토)
- [ ] 게이트웨이 폴링 수신/실패 카운트 표(`gateway_samples`) — 연결 끊김 vs 상태 안정 구분 (서지안 제안, 스키마는 최민준)
- [ ] 가동률 정의 통일: 상태창·대시보드 `todayUptime` → 분석 정의(계획 시간 분모) (서지안 제안)
- [ ] 설비 방향(회전) 필드 + 4방향 렌더 `<type>_{0,90,180,270}.png` (한도윤 제안, 맵 에디터·로더는 최민준)
- [ ] 상태별 애니메이션 프레임(RUN 시 공정품 이동 2~3장) — 로더 프레임 훅 필요 (한도윤 제안)
- [ ] 유형별 OEE 집계·알람 대응 시간을 OEE 표에 합치기 (서지안 제안)
- [ ] `generic` 실사 스프라이트(회색 캐비닛) — 큐브 폴백 유지 여부 결정 (한도윤 질문)
- [ ] 타일 2칸 설비 `footprint {w,h}` (한도윤 제안, 매니페스트 계약 변경)
