# handoff — 한도윤 (설비 3D 아티스트)

## 스프린트 2 라운드 1 (BLDC 신규 유형 8종 + 분해도 썸네일) — 2026-09-09 완료

### 한 일
1. **`assets/blender/build_equipment.py`** — 인터페이스 §9 신규 유형 8종 절차 모델 추가(기존 7종 그대로, 총 15종). 전부 §2 규격(192×192, 앵커 96,150, 카메라 60°/45°), 회색 프레임·황색 안전 요소·적색 비상정지·경광등 몸통 포함.
   | type | 형태(실제 설비 반영) |
   |---|---|
   | `stacker` | 백색 기둥형 본체 + 크라운·수직 실린더 + 사각 적층 램 + 가이드 포스트 4 + 테이블 위 **적층 코어(강판 줄무늬)** + 라미네이션 매거진 + HMI + 라이트커튼 |
   | `winder` | 청록 벤치 + 좌측 **스핀들 헤드스톡** + 스핀들 위 스테이터(강판 링+**구리 권선**) + 우측 **니들 헤드 기둥·갠트리·Z슬라이드** + 후방 구리 와이어 스풀 3 |
   | `vpi` | 스테인리스 **원통 탱크**(플랜지 밴드·돔 뚜껑·측면 진공 포트) + 리프트 포스트·암 + **진공 펌프 유닛**(청색 모터)+**진공 배관** + 바니시 드럼·이송관 + 사이트글라스·압력계 |
   | `oven` | 웜그레이 **챔버 + 스테인리스 도어**(내부 열 발광 창·핸들·힌지) + 상부 배기 스택·순환 송풍기 + 적색 고온 밴드 + 측면 제어반 |
   | `magnetizer` | 회색 **캐패시터 캐비닛**(계기·충전 램프·환기 슬릿·황색 고전압 밴드) + 벤치 위 **구리 착자 코일 헤드** + 삽입된 로터 + 굵은 방전 케이블 2 |
   | `balancer` | 청회색 저상 베드 + **롤러 페데스탈 2기** + 걸쳐 놓은 로터(샤프트+코어) + 구동 모터 헤드 + 뒤로 젖혀 연 **투명 안전 후드**(황색 힌지·레일) + 측정 콘솔 + 폴 경광등 |
   | `dispenser` | 백색 벤치 + Y레일·X브리지 **XY 갠트리** + Z헤드·**시린지·니들** + 회전 지그 위 로터 + 마그넷 트레이 + 접착제 압력 탱크·호스 + HMI + 황색 볼라드 |
   | `smt` | 백색 **통과형 캐비닛**(청색 밴드) + 양끝 입·출구 컨베이어(+X 개구·녹색 기판) + 전면 **피더 뱅크·릴 6개** + 상부 후드·검은 창 + 모니터 암 |
   - 재질 추가: `copper`(권선·코일), `teal`(와인더), `oven`(챔버), `clear`(투명 후드, alpha 0.3 + 투명 그림자), `heatglass`(건조로 창 발광).
2. **`assets/blender/postprocess.py`** — TYPES 15종, 주석 갱신(최민준 부탁): "게임 임계값 64(SPRITE_HIT_ALPHA) → 그림자 28~60 통과, 본체·윤곽 ≥ 64". 로직 변경 없음.
3. **`assets/blender/render.cmd`** — 주석에 15종·부분 렌더 안내. 동작 동일.
4. **`assets/blender/contact_sheet.py`**(신규) — 육안 검증용 컨택트 시트(3배 확대, 다크/라이트 배경, 마름모·램프 원·labelY 선 오버레이). `--work`로 후처리 전 본체도 볼 수 있음. 산출은 `assets/blender/out/`(커밋 제외).
5. **`assets/blender/make_part_thumbs.py`**(신규) — 분해도 9장 → HUD 썸네일. 원본 `public/assets/parts/<PN>.png`은 **수정하지 않음**(크기·바이트 동일 확인).
6. **산출물(커밋 대상)**
   - `public/assets/equipment/{stacker,winder,vpi,oven,magnetizer,balancer,dispenser,smt}.png` 8장 신규 + 기존 7장 재렌더(좌표·bbox 지난 라운드와 동일) + `manifest.json` 15종. PNG 15장 합계 **79.8 KB**.
   - `public/assets/parts/thumb/{SA-1000,RA-2000,HS-3010,BP-3020,FS-7010,FS-7020,GB-8000,PL-9000,PE-6000}.png` 9장, 합계 86.5 KB.

### 매니페스트 15종 (public/assets/equipment/manifest.json) — lamp는 경광등 몸통 중심의 정확한 투영
| type | lamp (x,y) | 컨테이너 좌표 (lamp−anchor) | labelY | 불투명 bbox (x0,y0,x1,y1) | 파일 |
|---|---|---|---|---|---|
| press | 110, 86 | (14, −64) | 70 | 70, 81, 122, 161 | 5,798 B |
| welder | 114, 120 | (18, −30) | 93 | 69, 103, 121, 157 | 5,206 B |
| robot | 119, 123 | (23, −27) | 99 | 64, 109, 126, 157 | 5,502 B |
| assembly | 107, 105 | (11, −45) | 89 | 73, 99, 118, 162 | 5,117 B |
| inspector | 106, 97 | (10, −53) | 81 | 77, 92, 115, 158 | 4,989 B |
| packer | 91, 112 | (−5, −38) | 96 | 68, 106, 117, 160 | 4,749 B |
| cnc | 112, 103 | (16, −47) | 74 | 70, 84, 126, 162 | 5,917 B |
| **stacker** | 99, 74 | (3, −76) | 58 | 74, 69, 118, 160 | 6,151 B |
| **winder** | 107, 103 | (11, −47) | 84 | 72, 94, 120, 161 | 5,649 B |
| **vpi** | 106, 80 | (10, −70) | 64 | 71, 74, 122, 162 | 6,325 B |
| **oven** | 115, 101 | (19, −49) | 74 | 72, 84, 121, 161 | 5,463 B |
| **magnetizer** | 98, 96 | (2, −54) | 80 | 72, 90, 120, 161 | 4,729 B |
| **balancer** | 116, 118 | (20, −32) | 91 | 71, 101, 122, 164 | 5,627 B |
| **dispenser** | 113, 124 | (17, −26) | 100 | 74, 110, 118, 160 | 4,680 B |
| **smt** | 94, 96 | (−2, −54) | 80 | 71, 91, 121, 161 | 5,864 B |

- 기존 7종의 lamp·labelY·bbox는 2026-09-07 값과 동일(회귀 없음). `labelY = max(8, min(top-10, lamp.y-16))` 규칙 그대로. stacker가 가장 높다(top y=69, 규격 상한 10 여유).

### 분해도 썸네일 규격 (public/assets/parts/thumb/<PN>.png) — 최민준 HUD용
- **96×96 px RGBA, 전 픽셀 불투명**, 흰 배경 유지(원본 카드와 같은 톤), **1px 회색 테두리 rgb(150,154,164)** — 다크(#1e2230)·라이트(#f4f4f6) 양쪽에서 카드 경계가 보임(시트로 확인).
- 카드 여백 트리밍: 흰 카드(약 260px) 안의 콘텐츠 bbox를 긴 변 기준 정사각 + 8% 여백으로 잘라 LANCZOS 축소. 부품이 썸네일의 약 84%를 채운다(원본은 약 45%).
- 파일명 = 원본과 동일(`<PN>.png`). `parts.image`에 원본 파일명이 들어가면 썸네일 경로는 `/assets/parts/thumb/` + image 로 그대로 파생 가능.
- 용도 제안: 상태창 투입 단품 표 썸네일(표시 32~48px로 축소해도 선명), 분해도 모달은 원본(325×484) 사용.
- 재생성: `python assets/blender/make_part_thumbs.py` (원본만 있으면 됨, Blender 불필요).

### 검증 결과 (2026-09-09, 이 PC)
- `render.cmd` 15종 전체: Blender EEVEE 렌더 31장(캘리브레이션 1 + 본체 15 + 그림자 15) 전부 OK, 약 25초. postprocess 검증 15종 OK(exit 0).
- 캘리브레이션 마름모 실측 **64×32 px, 중심 (96.0, 150.0)**, 오프셋 (0,0). meta 투영 원점 (96,150)·꼭짓점 (96,166)(128,150)(96,134)(64,150).
- 매니페스트: version 1, tile 64×32, anchor (96,150), types 15(순서 press…cnc, stacker…smt), file 전부 존재·192×192 RGBA.
- 알파 통계(15장 모두): **알파 1~27 픽셀 0개**, 그림자 밴드 28~63 픽셀 1,197~1,901개/장(최대 60, balancer 58), **본체 최소 알파 96~111(≥ 64)**, 완전 불투명(255) 1,068~2,786개/장 — 64~95 구간 픽셀 없음 → §3 클릭 규칙(그림자 통과·본체 히트)과 여유 있게 분리. 램프 좌표 픽셀 알파 15종 모두 255(몸통 위).
- 육안(3배 컨택트 시트, 다크·라이트): 8종 형태 구분 명확(탱크·챔버+굴뚝·캐비닛+코일·저상 베드+후드·벤치 갠트리·통과형 캐비닛+릴·기둥형 적층기·스핀들+스풀), 배경 투명, 검은 사각형 없음, 윤곽 1px로 라이트 배경에서도 실루엣 유지, 램프 원이 경광등 몸통 위, labelY 선이 최고점 위.
- 수정 이력: 밸런서 후드를 닫힌 상자에서 **뒤로 젖혀 연 패널**로 변경(로터·페데스탈이 가려져 밸런서로 안 읽힘), 구리 재질 밝기 상향(갈색으로 보임).
- 썸네일 9장: 96×96 확인, 원본 9장 크기 불변. 시트로 다크·라이트 테두리 확인.
- 게임 화면 실제 로드는 **미확인**(서버·공용 파일은 최민준 담당, 이번 라운드 서버에 의존하지 않음).

### 최민준에게 (라운드 2 통합)
1. **로더**: 매니페스트 15종 그대로 두었다(키·형식 동일). §9 유형 8종을 `EQUIPMENT_TYPES`·콘솔 라벨에 등록하면 BLDC 배치 24대가 큐브 없이 전부 이미지로 떠야 한다. 다크·라이트에서 램프가 경광등 몸통 위인지 확인 부탁 — 어긋나면 유형·px를 알려 달라(스크립트에서 재생성).
2. **썸네일 경로**: 상태창 투입 단품 표는 `/assets/parts/thumb/<pn>.png`(96×96), 🔩 분해도 모달은 원본 `/assets/parts/<pn>.png`. §8(최민준 소유)에 썸네일 경로 한 줄 추가 부탁. 파일이 없는 P/N은 썸네일도 없으니 폴백(빈 칸 또는 P/N 텍스트) 처리.
3. **커밋 대상**: `public/assets/equipment/*.png`(15) + `manifest.json`, `public/assets/parts/thumb/*.png`(9), `assets/blender/{build_equipment.py, postprocess.py, render.cmd, contact_sheet.py, make_part_thumbs.py}`. `assets/blender/out/`은 기존 .gitignore 그대로.
4. **라벨 겹침**: BLDC 배치는 24대가 촘촘할 수 있다. stacker(labelY 58)·vpi(64)처럼 키 큰 설비가 뒤 타일 설비의 라벨을 가리면 알려 달라 — 매니페스트 labelY를 유형별로 조정한다.

### 서지안에게
- `TYPE_LABEL` 8종(적층기·와인더·함침조·건조로·착자기·밸런싱 머신·디스펜서·SMT)은 서지안이 이미 넣었다(협업로그 22:45). 문구 이의 없음 — 콘솔 라벨(최민준)이 확정되면 그쪽에 맞추면 된다.

### 열린 질문 / 다음 개선안
- SMT 컨베이어 다리가 타일 밖 x=±1.03까지 나간다(3%). 인접 타일과 겹쳐 보이면 `footprint {w:2,h:1}` 계약을 제안하겠다.
- 밸런서 후드가 열린 상태라 RUN 상태에서는 닫힌 후드 프레임이 맞다 — 상태별 프레임 훅이 생기면 `balancer_run.png`로 분리.
- Cycles(`render.cmd --engine cycles`, CPU 64샘플)로 구리·스테인리스 반사 품질 상향 가능. 현재 EEVEE도 규격 충족.
- 재현: `assets\blender\render.cmd`(전체) / `render.cmd --types stacker,winder`(부분, 나머지 유형 매니페스트 유지) / `python assets\blender\make_part_thumbs.py`.

## 라운드 2 (설비 스프라이트 7종) — 2026-09-07 완료

### 한 일
1. **`assets/blender/build_equipment.py`** (Blender 5.2.1 bpy, 배치 실행)
   - 설비 7종 절차 모델: press(C-프레임+상하 램+플라이휠+라이트커튼), welder(청색 전원 캐비닛+피더+가스통+토치·호스+작업대+황색 차광막),
     robot(6축 주황 팔+베이스+제어반+황색 펜스), assembly(녹색 벨트 컨베이어+양끝 롤러+갠트리 헤드+황색 가드+페데스탈),
     inspector(백색 비전 게이트+카메라·링라이트+모니터), packer(투입 터널+개구+골판지 상자+필름 롤), cnc(밀폐 외장+검은 창+펜던트+툴 매거진 타워+칩 컨베이어).
     전 유형에 회색 프레임·황색 가드·적색 비상정지·경광등 몸통 포함.
   - 카메라 직교 (60°, 0, 45°), `ortho_scale = 3·타일·√2`(프레임 폭 = 타일 3칸 → 마름모 64px), `shift_y = +0.28125`(원점 → (96,150)).
   - 조명: 키 선(그림자 O, 카메라 왼쪽 앞) + 필 선 2개(그림자 X) + 월드 주변광. 뷰 변환 Standard(채도 유지).
   - 엔진: EEVEE(5.2 식별자 `BLENDER_EEVEE`, enum_items로 자동 선택). `--engine cycles`면 Cycles 애드온을 켜서 CPU 64샘플.
   - 유형별 2패스: `_body.png`(본체, 투명) + `_shadow.png`(설비를 `visible_camera=False`·holdout으로 숨기고 바닥만 → 그림자 휘도).
   - `meta.json`: 램프·원점·타일 꼭짓점의 정확한 픽셀 투영(`world_to_camera_view`), 유형별 최고점.
2. **`assets/blender/postprocess.py`** (시스템 python + PIL + numpy)
   - `_calib.png`(타일 1칸 평면) 측정으로 마름모 64×32·중심 (96,150) 검증. 어긋나면 정수 오프셋으로 전 스프라이트 정렬(현재 오프셋 0).
   - 본체 실루엣 1px 어두운 윤곽(알파 225) — 라이트/다크 테마 대비.
   - 그림자: 바닥 렌더 휘도 → 알파 **28~60** 밴드 + 앵커 중심 접지 타원(52→28). 알파 1~23 픽셀 **0개**(게임 pixelPerfect 24 기준 통과 없음).
   - 검증(실패 시 exit 1): 빈 이미지·평균 밝기(검은 사각형)·캔버스 잘림·상단 y<10·앵커 주변 불투명·합계 2MB.
   - `manifest.json` 생성(부분 렌더 시 기존 유형 유지).
3. **`assets/blender/render.cmd`**: Blender 배치 → postprocess 한 번에. `render.cmd --types press,cnc`, `--engine cycles`, `--samples N` 통과.
4. **산출물(커밋 대상)**: `public/assets/equipment/{press,welder,robot,assembly,inspector,packer,cnc}.png` + `manifest.json`. 합계 36.4 KB.

### 매니페스트 (public/assets/equipment/manifest.json)
| type | lamp (x,y) | labelY | 불투명 bbox (x0,y0,x1,y1) |
|---|---|---|---|
| press | 110, 86 | 70 | 70, 81, 122, 161 |
| welder | 114, 120 | 93 | 69, 103, 121, 157 |
| robot | 119, 123 | 99 | 64, 109, 126, 157 |
| assembly | 107, 105 | 89 | 73, 99, 118, 162 |
| inspector | 106, 97 | 81 | 77, 92, 115, 158 |
| packer | 91, 112 | 96 | 68, 106, 117, 160 |
| cnc | 112, 103 | 74 | 70, 84, 126, 162 |

- `lamp`는 경광등 몸통(검은 원기둥) 중심의 정확한 투영 좌표. 게임이 그리는 반지름 5 원이 몸통 위에 얹힌다(3배 확대 시트로 7종 확인).
- `labelY = max(8, min(top-10, lamp.y-16))` — 라벨 12px 텍스트가 설비 최고점·램프 광원(r9)과 겹치지 않는 값.
- `anchor`(96,150), `tile`(64×32)는 규격 그대로.

### 검증 결과 (2026-09-07, 이 PC)
- `render.cmd` 전체 실행 약 15초(EEVEE, AMD 내장 GPU). 렌더 14장 + 캘리브레이션 1장 전부 OK.
- 캘리브레이션 마름모 실측 **64×32 px, 중심 (96.0, 150.0)**, meta 투영 원점 (96,150)·꼭짓점 (64,150)(128,150)(96,134)(96,166) 일치.
- PNG 7장 Read 도구로 육안 확인: 형태 구분 명확, 배경 투명, 검은 사각형 없음. 3배 확대 컨택트 시트(다크 #1e2230 / 라이트 #f4f4f6 배경, 타일 마름모·램프 원·라벨선 오버레이)로 정렬·램프·테마 대비 확인.
- 알파 통계: 알파 1~23 픽셀 0개(7장 모두), 본체 알파 255 픽셀 1,068~2,786개/장.
- 게임 화면 실제 로드는 **미확인**(공용 파일·서버 세션은 최민준 담당). 파일만 두면 로더가 읽는 구조라 라운드 3에서 확인 요청.

### 최민준에게 (라운드 3)
1. **로더 확인**: `public/assets/equipment/` 에 7종 PNG + manifest 배치 완료. 게임 맵에서 11대 전부 이미지 스프라이트로 바뀌는지, 라이트 테마·저사양 모드에서 확인 부탁.
2. **램프 좌표**: 표의 lamp 값이 경광등 몸통 위에 오는지 실제 화면에서 한 번 봐 달라. 어긋나면 어느 유형이 몇 px인지 알려주면 모델을 옮긴다(좌표를 손으로 고치지 말고 스크립트에서 재생성).
3. **라벨**: labelY는 설비 상단 10px 위. 라벨이 이웃 타일 설비와 겹치면 게임 쪽에서 labelY를 더 올리는 것보다 매니페스트 값을 조정하는 편이 낫다 — 원하는 오프셋을 알려 달라.
4. **.gitignore**: `assets/blender/out/`(중간 렌더·meta) 추가 요청. `.blend`는 만들지 않는다(스크립트 재생성).
5. **인터페이스 §2 카메라 각도**: 63.435° → **60°** 로 고쳤다(§2는 내 소유). 이유: 카메라 X회전 rx의 바닥 마름모 종횡비는 1/cos(rx). 63.435°는 2.236:1(마름모 높이 28.6px), 60°가 정확히 2:1(64×32). 캘리브레이션 실측으로 확인. 다른 문서(README 등)에 63.435°가 인용돼 있으면 함께 고쳐 달라.
6. **클릭 영역**: 그림자 알파를 28 이상으로 둬서 그림자도 클릭된다(규격대로). 현장에서 그림자 클릭이 거슬리면 postprocess의 `SHADOW_A_MIN`을 20으로 내리면 그림자는 통과된다 — 정책 결정 부탁.

### 열린 질문
- 유형 추가 제안 없음(7종으로 충분). 다만 `generic`도 "회색 캐비닛" 실사 스프라이트로 만들면 큐브 폴백이 사라지는데, 폴백을 남길지 결정 필요.
- 설비 방향: 모든 설비의 정면이 화면 왼쪽 아래(-Y)를 향한다. 맵에서 배치 방향(회전) 필드가 생기면 4방향 렌더(`<type>_{0,90,180,270}.png`)로 확장 가능.

### 다음 개선안
- Cycles 렌더(`--engine cycles`, CPU 64샘플, 약 1~2분)로 AO·반사 품질 상향 — 현재 EEVEE도 규격 충족.
- 상태별 변형(RUN 시 벨트 위 공정품 이동 프레임 2~3장) — 로더가 애니메이션 프레임을 받을 훅이 필요.
- 타일 2칸 설비(긴 컨베이어) — 매니페스트에 `footprint {w,h}` 추가 제안 후 진행.
- 재현: `assets\blender\render.cmd` (Blender 5.2 + python(PIL, numpy) 필요). 다른 PC에 Blender가 없어도 PNG는 커밋되어 있어 게임은 동작.
