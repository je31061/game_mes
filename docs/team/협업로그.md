# 협업로그 (append only — 최신이 아래)

형식: `- YYYY-MM-DD HH:MM · 발신 → 수신 · [질문|답변|결정|요청|알림] 내용`

- 2026-09-07 00:00 · PM → 전원 · [결정] 스프린트 1 시작. 역할·계약은 운영규약.md·인터페이스.md. 라운드 1은 최민준 단독, 라운드 2는 한도윤·서지안 병행(공용 파일 미수정), 라운드 3은 최민준 통합.
- 2026-09-07 00:00 · PM → 한도윤 · [알림] 이 PC에 Blender 5.2가 설치되어 있다(`C:\Program Files\Blender Foundation\Blender 5.2\blender.exe`). PIL(Pillow)도 사용 가능. 렌더 산출물은 커밋 대상.
- 2026-09-07 00:00 · PM → 서지안 · [알림] 현재 DB에는 상태 로그 약 200건, 작업지시·실적은 소수. 값이 비어도 화면이 깨지지 않게 "데이터 없음" 처리를 요구한다.
- 2026-09-07 22:30 · 최민준 → 한도윤, 서지안 · [알림] 라운드 1(기반) 완료 — 시작하세요. `equipments.type` 컬럼(기존 DB 11대 매핑 완료), 설비 API/배치 JSON/init/world:refresh에 `type` 포함, 관리자 콘솔 유형 select, 게임 스프라이트 로더, 분석 탭 훅, 빈 매니페스트 준비. 상세·검증 수치는 `docs/team/handoff-최민준.md`.
- 2026-09-07 22:30 · 최민준 → 한도윤 · [알림] 훅 위치: `public/assets/equipment/manifest.json`(현재 `types: {}`)과 같은 폴더의 `<type>.png`만 놓으면 게임이 읽는다. 게임이 쓰는 키는 `anchor{x,y}`(최상위, 기본 96,150), `types[type].file`(필수)·`lamp{x,y}`·`labelY`. 클릭은 불투명 픽셀 기준(pixelPerfect, 알파 24 미만 무시). 유형별 파일이 없으면 큐브로 폴백하므로 부분 납품 가능. 유형 추가 제안은 협업로그에 남기면 db.js·admin.js를 내가 고친다.
- 2026-09-07 22:30 · 최민준 → 서지안 · [알림] 훅 위치: `server/index.js`가 `import('./analytics.js')`로 `registerAnalytics(app, { requireAdmin, db, queries, settings })`를 호출(파일 없으면 로그만). `public/admin.html`에 탭 `data-tab="analytics"`·`#tab-analytics`·`css/analytics.css`·`js/analytics.js`가 걸려 있고, `public/js/admin.js`가 탭 활성화마다 `window.FWAnalytics.mount($('tab-analytics'), api)`를 호출한다(멱등하게 작성). 두 파일이 없을 때는 서버가 빈 JS/CSS를 200으로 대신 주니 파일을 두는 순간 자동 전환. `queries.listEquipments`에 `type`이 있으니 OEE 응답의 `type`은 그 값을 쓰면 된다.
- 2026-09-07 23:40 · 한도윤 → 전원 · [제안→반영] 인터페이스 §2 카메라 X회전 63.435° → **60°**. 마름모 종횡비 = 1/cos(rx)라 63.435°는 2.236:1(높이 28.6px), 60°가 정확히 2:1(64×32). `_calib.png` 실측 64×32·중심 (96,150)으로 확인. §2는 내 소유라 문구를 고쳤다. 영향: README 등에 63.435°가 인용돼 있으면 최민준이 함께 수정.
- 2026-09-07 23:40 · 한도윤 → 최민준 · [알림] 라운드 2 완료 — `public/assets/equipment/` 에 7종 PNG(합계 36.4 KB) + `manifest.json`(lamp·labelY 포함) 배치. 스크립트 `assets/blender/{build_equipment.py, postprocess.py, render.cmd}`. EEVEE(`BLENDER_EEVEE`) 약 15초. 상세·좌표 표·검증 수치는 `docs/team/handoff-한도윤.md`. 커밋은 하지 않았다(라운드 3 통합 커밋).
- 2026-09-07 23:40 · 한도윤 → 최민준 · [요청] (1) 게임 맵에서 11대 이미지 스프라이트 전환·램프가 경광등 몸통 위에 오는지·라벨 위치를 실제 화면(라이트 테마·저사양 포함)에서 확인. 어긋나면 유형과 px를 알려 달라, 스크립트에서 재생성한다. (2) `.gitignore`에 `assets/blender/out/` 추가. (3) 그림자 알파를 28~60으로 둬서 그림자 영역도 클릭된다(pixelPerfect 24 기준) — 그림자 클릭 통과를 원하면 알려 달라(`SHADOW_A_MIN` 조정). (4) 로더 변경 요청 없음: 현재 훅(`types[type].file/lamp/labelY`, 최상위 `anchor`)으로 충분.
- 2026-09-07 23:05 · 서지안 → 최민준, 한도윤 · [알림] 라운드 2(분석) 완료. `docs/분석-정의.md`, `server/analytics.js`(라우트 3개), `public/js/analytics.js`, `public/css/analytics.css`. 서버 재기동 후 관리자 토큰으로 3개 API 호출·WLD-01/ASM-01/PRS-01 수기 계산 대조 일치, 📈 탭 다크·라이트 확인. 상세·응답 예는 `docs/team/handoff-서지안.md`. 공용 파일은 손대지 않았고 커밋도 하지 않았다.
- 2026-09-07 23:05 · 서지안 → 최민준 · [요청] 운영 정책에 `policy.shiftMinutesPerDay`(정수 1~1440, "1일 계획 가동 시간(분)") 입력란·저장 검증 추가. 분석 API는 이미 읽고 있어 없으면 1440(24h)으로 동작하므로 급하지 않음. README·운영 가이드에 📈 탭 한 줄과 `docs/분석-정의.md` 링크도 부탁.
- 2026-09-07 23:05 · 서지안 → 최민준 · [제안] 게임 상태창 `todayUptime`(첫 로그 이후만 분모)과 분석 탭 가동률(계획 시간 분모)은 정의가 달라 숫자가 다르게 보인다. 상태창 라벨을 "로그 구간 기준"으로 구분하거나, 다음 스프린트에 분석 정의로 통일하는 안 검토 요청. 또 게이트웨이 폴링 수신/실패 카운트 표(스키마)는 다음 스프린트 제안으로 handoff에 적어 두었다.
- 2026-09-07 23:05 · 서지안 → 한도윤 · [알림] 분석 응답의 `type`은 `equipments.type` 그대로 쓰고 화면 라벨만 붙인다(유형 추가 시 `public/js/analytics.js`의 `TYPE_LABEL`에 한 줄 추가 — 내가 한다). 유형이 늘면 협업로그에 남겨 주면 된다. 검증 중 서버를 두 번 재기동했다(23:00 전후).
- 2026-09-08 21:30 · 최민준 → 한도윤 · [답변] 요청 4건 반영 결과. (1) 로더 확인: 11대 전부 이미지 스프라이트 전환 확인(다크·라이트·저사양 30fps). 램프 컨테이너 좌표는 매니페스트−앵커와 일치(press (14,−64), welder (18,−30), robot (23,−27), assembly (11,−45), inspector (10,−53), packer (−5,−38), cnc (16,−47)). 3배 컨택트 시트(다크/라이트 배경, 마름모·램프·라벨선 오버레이)로 7종 모두 램프가 경광등 몸통 위, 라벨선이 최고점 위임을 확인 — **재생성 요청 없음, 좌표 그대로 간다.** 라벨은 현재 배치(이웃 타일 인접 없음)에서 겹침 없음. (2) `.gitignore`에 `assets/blender/out/` 추가(이번 커밋). (3) 카메라 각도: README·운영 가이드·브리프 어디에도 63.435° 인용 없음 — 정정 대상 없음. §2의 60° 정정 수용. (4) 로더 변경 없음 확인.
- 2026-09-08 21:30 · 최민준 → 한도윤, 서지안 · [결정] **그림자 클릭은 통과한다.** 현장 태블릿에서 그림자를 눌러 엉뚱한 설비가 열리는 오클릭이 더 해롭다고 판단. 매니페스트·`SHADOW_A_MIN`은 손대지 않고 로더 쪽 임계값을 올렸다: `game.js` `SPRITE_HIT_ALPHA = 64`(알파 64 미만 통과). 인터페이스 §3에 계약으로 적음 — 앞으로 그림자는 알파 63 이하, 본체·윤곽은 64 이상으로 렌더. 검증: 7종 그림자 픽셀(알파 28~60, 295~430개/장) 히트 0, 본체 픽셀(255, 268~697개/장) 히트 100%. 한도윤에게 부탁: `postprocess.py` 주석 "게임 alphaTolerance 24 → 최소 28"을 "게임 임계값 64 → 그림자 28~60은 통과, 본체 ≥ 64"로 다음 라운드에 고쳐 달라(파일은 한도윤 소유라 내가 안 건드림).
- 2026-09-08 21:30 · 최민준 → 서지안 · [답변] 요청 반영 결과. (1) 운영 정책 카드에 "1일 계획 가동 시간(분)" 입력란(`#pol-shift`, 1~1440, 비우면 24h) + `PUT /api/admin/policy` 서버 검증. 경계값 확인: 0·2000·480.5·'abc' → 400, 480 → 200 후 OEE `range.shiftMinutesPerDay 480 / shiftConfigured true`, WLD-01(08-13) 가동률 101.1/480 = 21.06%로 바뀜. 보존 기간만 보내도 shift 값 유지, null이면 1440 복귀. 검증 후 정책은 원래대로(null) 복원. (2) README 관리자 콘솔 절·스프린트 1 절·운영 가이드 §3/§3-1/체크리스트에 📈 탭 설명과 `docs/분석-정의.md` 링크 추가. (3) 상태창 라벨 "금일 가동 비율 (로그 구간 기준)"(툴팁에 분모 차이 설명), 대시보드 점유율 표 "가동률 (로그 구간)"으로 구분. 정의 통일은 스프린트 2 후보로 올림. (4) 게이트웨이 폴링 카운트 표 — 스프린트 2 후보 1순위로 handoff에 적었다. 산출물 4개 규격 일치, 되돌린 것 없음.
- 2026-09-08 21:30 · 최민준 → 전원 · [알림] 라운드 3(통합) 완료. 검증: `node --check` 21파일 통과, 📈 탭 라이트 테마 렌더(요약 타일·OEE 막대 11대·표 3개), 설비 마스터 유형 select 11행 DB 일치·신규 8종, 부하 테스트 50명 30초 통과(이동 p95 21.1ms·손실 1건, 채팅 p95 10.1ms). 두 사람 산출물 포함 커밋·push(해시는 handoff-최민준.md). 다음 스프린트 후보는 handoff-최민준.md "스프린트 2 후보" — 각자 의견을 협업로그에 남겨 달라.
- 2026-09-09 22:10 · PM → 전원 · [결정] 스프린트 2 시작: BLDC 500W 모터 라인(공정 24·단품 49·분해도 9). 원천 `docs/bldc/bldc-500w-48v.json`·`public/assets/parts/*.png`·`docs/bldc/layout-bldc-500w.json` 준비 완료. 계약은 인터페이스 §7·§8·§9. 라운드 1은 세 명 병행(소유권 준수), 라운드 2는 최민준 통합. 새 유형 8종 스프라이트는 한도윤, 라인 밸런스 분석은 서지안, 스키마·API·HUD·라인 전환은 최민준.
- 2026-09-09 22:10 · PM → 한도윤 · [알림] 분해도 이미지는 PDF에서 PDF 원본 순서대로 추출해 P/N으로 이름 붙였다(SA-1000, RA-2000, HS-3010, BP-3020, FS-7010, FS-7020, GB-8000, PL-9000, PE-6000). 흰 카드 배경 325×484. 원본은 건드리지 말고 썸네일이 필요하면 별도 폴더에.
- 2026-09-09 22:10 · PM → 서지안 · [알림] BOP 시트에는 조립 라인 공정이 14개(B85·B87 신규 공정 포함)라 총 24공정이다(사양 시트의 "22"는 구 버전). 라인 밸런스 시트의 가동 20h/일·OEE 0.85·병목(A40 45초 병렬 2대 후, B110 60초)을 기준값으로 문서화.
- 2026-09-09 22:45 · 서지안 → 최민준, 한도윤 · [알림] 스프린트 2 라운드 1(라인 밸런스) 완료. `docs/분석-정의.md` §8, `server/analytics.js`에 `GET /api/admin/analytics/line-balance?product&hours=20&oee=0.85&targetUph=60&parallel[OP]=n&hints=1`, `public/js/analytics.js` 📈 탭 "라인 밸런스" 카드(제품 타일·라인별 타일·공정 C/T 막대·표·병렬 입력), css. 공용 파일 미수정, 커밋 안 함, **서버 재기동 안 함**(최민준이 22:19 이후 재기동한 서버에 새 analytics.js가 이미 실려 있었다). 검증: JSON 원천 50항목 + 인메모리 sqlite 라우트 + 실서버(관리자 세션 토큰) + 실제 📈 탭 렌더 — xlsx 12_Line_Balance와 일치(아마추어 A40 45초·UPH 80·일 1360, 조립 B110 60초·UPH 60·일 1020, 설비 연결 24/24). 상세는 `docs/team/handoff-서지안.md`.
- 2026-09-09 22:45 · 서지안 → 최민준 · [결정→문서] 병목 동률 규칙: 조립 라인 B90·B110이 60초 동률이라 "시트 `kind=병목` 표기 우선, 없으면 seq 앞선 것, 동률 목록은 `tiedWith`"로 정했다(§8.3). 배치 공정(A80·A90)은 xlsx처럼 합계·병목에서 빼되 개당 C/T(3600/30 = 120초/ea)가 병목 45초보다 커서 경고를 낸다 — 숫자는 xlsx와 같고 경고만 추가. 이의 있으면 협업로그에.
- 2026-09-09 22:45 · 서지안 → 최민준 · [요청] (1) README·운영 가이드 §3-1에 라인 밸런스 카드 한 줄 + `docs/분석-정의.md` §8 링크. (2) `GET /api/admin/bop`의 설비 연결 현황이 내 `unlinked`(equipments.op = processes.op, hidden 제외)와 같은 정의인지 대조(지금은 둘 다 24/24). (3) 정책 질문: 같은 op에 설비 2대를 등록해도 `parallel`은 자동 반영하지 않고 `linkedCount 2`로만 표시한다 — 자동 반영을 원하면 알려 달라. (4) 주의: node:sqlite는 `ALTER TABLE` 전에 prepare한 `SELECT *`가 새 컬럼을 못 본다(테스트에서 확인). db.js는 마이그레이션 → `queries` 순서라 문제없음. (5) `hidden` 설비는 분석 3개 라우트에서도 제외했다(`listEquipments`가 이미 거르므로 중복이지만 무해). (6) 게이트웨이 표는 BLDC 24대에 `data_source`가 없어 비어 있다 — SIM을 붙일 계획이면 알려 달라.
- 2026-09-09 22:45 · 서지안 → 한도윤 · [알림] `public/js/analytics.js`의 `TYPE_LABEL`에 §9 새 유형 8종 라벨을 넣었다(적층기·와인더·함침조·건조로·착자기·밸런싱 머신·디스펜서·SMT). 라벨 문구가 매니페스트·콘솔과 다르면 알려 달라. 라인 밸런스 차트의 설비 상태 점은 게임 램프와 같은 색(`--run/--idle/--stop/--alarm`)을 쓴다.
- 2026-09-09 23:40 · 한도윤 → 최민준 · [알림] 스프린트 2 라운드 1 완료 — §9 신규 유형 8종(stacker, winder, vpi, oven, magnetizer, balancer, dispenser, smt) 스프라이트를 `public/assets/equipment/`에 추가, `manifest.json` **15종**(기존 7종 좌표 동일). PNG 15장 합계 79.8 KB. 검증: 캘리브레이션 64×32·(96,150), 15장 모두 알파 1~27 픽셀 0·그림자 28~60·본체 최소 96(≥64)·램프 픽셀 255, 3배 컨택트 시트 다크·라이트 육안 확인. `postprocess.py` 주석(24→64) 갱신 완료. 상세·램프 좌표 표는 `docs/team/handoff-한도윤.md`. 커밋은 하지 않았다.
- 2026-09-09 23:40 · 한도윤 → 최민준 · [알림] 분해도 썸네일 규격: `public/assets/parts/thumb/<PN>.png` 9장 — **96×96 RGBA, 전 픽셀 불투명, 흰 배경, 1px 회색 테두리 rgb(150,154,164)**(다크 테마에서 카드 경계 확보), 카드 여백 트리밍(부품이 약 84% 채움). 파일명은 원본과 동일하므로 `parts.image`에서 `/assets/parts/thumb/` + image 로 파생 가능. 원본 9장은 바이트 단위로 무변경. 재생성 `python assets/blender/make_part_thumbs.py`.
- 2026-09-09 23:40 · 한도윤 → 최민준 · [요청] (1) §9 8종을 `EQUIPMENT_TYPES`·콘솔 라벨에 등록 후 BLDC 배치 24대가 전부 이미지 스프라이트로 뜨는지, 램프가 경광등 몸통 위인지 다크·라이트에서 확인(어긋나면 유형·px를 알려 달라, 스크립트에서 재생성). (2) 인터페이스 §8(최민준 소유)에 썸네일 경로 한 줄 추가: 투입 단품 표 = `/assets/parts/thumb/<pn>.png`, 분해도 모달 = 원본. (3) 커밋 대상: equipment PNG 15 + manifest, parts/thumb PNG 9, `assets/blender/{build_equipment.py, postprocess.py, render.cmd, contact_sheet.py(신규), make_part_thumbs.py(신규)}`. (4) 24대가 촘촘하면 키 큰 stacker(labelY 58)·vpi(64) 라벨이 뒤 타일과 겹칠 수 있다 — 겹치면 알려 달라, 매니페스트 labelY를 조정한다.
- 2026-09-09 23:40 · 한도윤 → 서지안 · [답변] `TYPE_LABEL` 8종 문구(적층기·와인더·함침조·건조로·착자기·밸런싱 머신·디스펜서·SMT) 이의 없음 — 매니페스트에는 라벨이 없고 콘솔 라벨은 최민준이 §9 문구로 정하니, 최민준 라벨이 확정되면 그쪽에 맞추면 된다. 상태 점 색을 램프와 통일한 것도 좋다.
- 2026-09-10 00:20 · 최민준 → 한도윤, 서지안 · [알림] 스프린트 2 라운드 1(기반) 완료 — 스키마·API·훅 확정. (1) 스키마: `products / processes / parts / process_inputs` + `equipments.op`·`equipments.hidden`·`zones.hidden`(기존 DB 자동 마이그레이션, 멱등). §7 그대로. `EQUIPMENT_TYPES` 16종(§9 8종 등록, 콘솔 `TYPE_LABEL` 문구 = 인터페이스 §9: 자동 적층기·니들 와인더·진공 함침조 (VPI)·열풍 건조로·착자기·밸런싱 머신·접착 디스펜서·SMT · 자동 결선). (2) API: `POST /api/admin/bop/import`, `GET /api/admin/bop`, `GET /api/bop/exploded`, `POST /api/admin/bop/apply-sample`, 배치 JSON `op`·`replace`, 설비 API `op`·`hidden`, `equipment:detail.process`. (3) 숨김 규칙: `queries.listEquipments/listZones/listLinks/alarmEquipments`에 `hidden = 0` — 이 쿼리를 쓰는 곳은 자동 제외(서지안 라우트 포함). (4) **실제 DB에 BLDC 라인 적용 완료** — 설비 24 표시/11 숨김, 존 2/4, 공정 24·단품 56·투입 36·라인 23, 24/24 공정 연결. 게임에서 OP-A40 클릭 → 단품 표·분해도 강조 확인(다크·라이트), 근접 대화·상태 변경·라인 부하 정상. 상세·수치는 `docs/team/handoff-최민준.md`. 커밋은 내 파일 + PM 원천(docs/bldc, scripts/bldc, parts 원본)만, 두 사람 산출물은 라운드 2 통합 커밋. push는 라운드 2.
- 2026-09-10 00:20 · 최민준 → 한도윤 · [답변] 요청 (1) 확인 완료: BLDC 24대 전부 이미지 스프라이트(큐브 0), 다크·라이트 스크린샷에서 램프가 경광등 위, 라벨 겹침 없음(A라인 2칸 간격 포함) — 재생성·labelY 조정 요청 없음. (2) §8에 썸네일 경로 메모 추가(라운드 1은 원본을 CSS 축소, 라운드 2에 `thumb/<pn>.png` 우선·원본 폴백으로 전환). (3) 커밋 목록 확인 — 라운드 2 통합 커밋에 포함. (4) 해당 없음.
- 2026-09-10 00:20 · 최민준 → 서지안 · [답변] (2) `unlinked` 정의 동일(`equipments.op = processes.op`, hidden 제외) — 24/24 일치. (3) 병렬 자동 반영 안 함, `linkedCount`만 표시로 확정. (5) 중복 제외 무해, 그대로. (6) SIM은 이번 스프린트에 붙이지 않음(필요 시 맵 에디터에서 설비별 지정). (1) README·가이드 §3-1 라인 밸런스 문구는 라운드 2 통합 때 넣는다. 참고: xlsx 12_Line_Balance 기준값 원문이 `products.spec_json.lineBalance`(`GET /api/admin/bop` → `product.lineBalance`)에 있다. (4)의 node:sqlite prepare 순서 지적 고맙다 — db.js는 마이그레이션 → queries 순서 유지.
- 2026-09-10 00:20 · 최민준 → 전원 · [알림] 부수 수정: 다크 테마 `--input/--overlay/--toast-bg`가 자기 참조(`var(--input)`)라 무효 처리되어 상태창·패널 배경이 투명하던 문제를 `style.css`에서 고쳤다(초기 커밋부터 있던 버그). 분석 css가 이 변수를 쓰면 다크에서 배경이 생기니 화면이 달라 보일 수 있다 — 라운드 2에서 함께 확인.
- 2026-09-09 23:55 · PM → 최민준 · [결정] (1) `docs/bldc/source/`의 xlsx·PDF 원본은 커밋한다 — 사용자가 이 목적으로 제공했고 시트에 "가상 사양"으로 명시, 재현 가능성이 우선. (2) 서지안의 병렬 자동 반영 질문: 지금은 자동 반영하지 않고 `linkedCount` 표시 + 수동 `parallel`만 유지(설비 2대가 실제로 병렬인지 BOP만으로는 알 수 없음). (3) Render 배포는 재배포마다 DB가 초기화되므로, 빈 DB로 기동할 때 환경변수 `FW_SEED_PRODUCT_LINE=bldc`면 `apply-sample`과 같은 절차를 자동 적용해 데모가 BLDC 라인으로 바로 뜨게 하고, `render.yaml`에 그 값을 넣어라. 로컬 개발 DB(이미 적용됨)는 영향 없음.
- 2026-09-09 23:55 · PM → 전원 · [알림] 라운드 2(통합)는 최민준. 한도윤·서지안 산출물은 그대로 커밋 대상. 결과 push 후 Render 자동 재배포 → https://factory-world.onrender.com 에서 설비 클릭 → 단품 표시를 최종 확인한다.
- 2026-09-10 18:10 · 최민준 → 한도윤 · [답변] 라운드 2 반영 결과. 산출물 규격 검사 통과 — PNG 15장 192×192 RGBA·알파 1~27 픽셀 0·64~95 구간 0·본체 최소 96·램프 알파 255, 썸네일 9장 96×96 전 픽셀 불투명·테두리 rgb(150,154,164), 원본 9장 HEAD와 바이트 동일. **되돌린 것 없음.** (2) 상태창 투입 단품 표를 `/assets/parts/thumb/<pn>.png` 우선으로 바꿨다(`app.js` `PARTS_THUMB`, 표시 36×36). 로드 실패 시 원본 → 그것도 없으면 빈 칸 — 실제 요소로 두 단계 폴백 시험 통과. 분해도 모달은 원본. §8 문구 확정. (3) PNG 15·manifest·thumb 9·blender 스크립트 5개 전부 이번 통합 커밋에 넣었다(`assets/blender/out/`·`__pycache__/` 제외, `.gitignore`에 `__pycache__/` 추가). 로컬 게임에서 24대 전부 이미지, 매니페스트·PNG 요청 전부 200.
- 2026-09-10 18:10 · 최민준 → 서지안 · [답변] 라운드 2 반영 결과. 산출물 계약 일치(`registerAnalytics` 시그니처, 라우트 4개 `requireAdmin`, `FWAnalytics.mount`, 셀렉터 144개 전부 `.fwa-*`, 테마 변수만) — **되돌린 것 없음.** (1) README 관리자 콘솔 📈 항목·스프린트 2 절, 운영 가이드 §3-1에 라인 밸런스 카드 설명과 `docs/분석-정의.md` §8 링크(앵커 `#8-라인-밸런스-line-balance-스프린트-2`). (2) 빈 DB 격리 서버에서도 `/api/admin/bop` linked 24 = 라인 밸런스 `summary.linked` 24, UPH 60·일 1,020·A40 45초/UPH 80/1,360 그대로. (3) 병렬 자동 반영 안 함(PM 23:55 결정). 참고: 982px 화면에서 📈 탭 본문이 1,300px로 가로 스크롤되던 것은 내 `admin.html` `.content`(flex 항목 `min-width: auto`)가 라인 밸런스 표 폭에 밀린 탓이라 `min-width: 0`으로 고쳤다 — 서지안 파일 수정 불필요, 이제 표는 `.fwa-scroll` 안에서 스크롤된다.
- 2026-09-10 18:10 · 최민준 → 전원 · [알림] 스프린트 2 라운드 2(통합) 완료. PM 결정 (3) `FW_SEED_PRODUCT_LINE=bldc` 빈 DB 자동 적용 구현 — 격리 서버(3002)에서 자동 적용 로그·linked 24/24·설비 24 표시/10 숨김(빈 DB 시드가 10대)·재기동 시 건너뜀 확인, `render.yaml`에 설정. `node --check` 20파일, 로컬 게임에서 설비 클릭 → 썸네일·단품 표·분해도 모달(다크·라이트), 부하 테스트(BLDC 24대, 50명 30초) 이동 p95 18.9ms·채팅 p95 9.4ms 통과. 라운드 1 커밋과 통합 커밋을 push → Render 자동 재배포 확인 결과는 handoff-최민준.md "Render 반영 확인"에 적는다. 스프린트 3 후보도 같은 파일에.
- 2026-09-10 18:35 · 최민준 → 전원, PM · [알림] push `7f8f779..990cb62`(라운드 1 `6917e76` + 통합 `990cb62`). Render 배포 09:20:32Z success(push 후 약 2.5분) — 로그인 없이 `/js/app.js` 새 표식, `winder.png`·`thumb/SA-1000.png` 200, `/api/bop/exploded` 401(배포 전 404), 분석 `line-balance` 라우트 401(등록됨), 매니페스트 15종·PNG·썸네일·원본 전부 200 확인. BLDC 자동 시드는 로그인해야 보이므로 PM이 입장해 "설비 24대" 토스트와 설비 클릭 → 단품 표시를 최종 확인해 달라(절차는 handoff). 이 기록 커밋은 `[skip render]`로 재배포를 막았다.
- 2026-09-10 20:10 · PM → 최민준 · [알림] 사용자 요청 핫픽스를 PM이 직접 반영(공용 파일): (1) 세션 자동 복귀 — `GET /api/me`, 게임은 저장 토큰이 유효하면 로그인 생략(캐릭터 저장 시 바로 출근), 콘솔 [공장 맵으로]는 `/?as=admin`으로 관리자 세션 우선, 테마·저사양 전환은 `sessionStorage.fw.resumePos`로 같은 자리 복귀(브리핑 생략). 콘솔은 세션 확인 중 로그인 폼 숨김. 🚪 로그아웃(게임 상단·콘솔 사이드바). (2) 토큰에 `emp`(사번) 추가, `authUser`가 사번 불일치 토큰 거부 — Render처럼 DB가 초기화되는 배포에서 같은 id의 다른 사용자로 인증되는 것 방지(기존 emp 없는 토큰은 만료까지 허용). (3) `game.js` 부팅 경쟁 수정 — 장면 `create()` 전(스프라이트 로딩 중)에 도착한 소켓 이벤트가 `linkStates.set`에서 오류 나던 것: `FW.scene()`은 `ready` 후에만 반환, 그 전 호출은 `FW._sceneQueue`에 쌓았다가 `create()` 끝에서 적용. 검증: 격리 서버 3002(임시 DB·임시 비밀번호)에서 /api/me 5종, 콘솔 테마 전환·콘솔→맵·게임 테마/저사양 전환(위치 7.5,5.5 동일 복귀)·로그아웃, 재부팅 12회 중 오류 0. 인터페이스 변화 없음.
- 2026-09-10 21:00 · PM → 전원 · [알림] 사용자 요청으로 사용설명서 `docs/manual.html`(GitHub Pages 공개, 인쇄용 A4 스타일) 작성, 문서 목록·README에 링크. 화면·권한·포인트·파일 형식 등 수치는 현재 코드 기준. 기능이 바뀌면 이 문서도 같이 고친다 — 최민준(공용 UI)·서지안(분석 탭)·한도윤(설비 유형) 변경 시 해당 절 갱신 요청을 협업로그에.
- 2026-09-12 09:00 · PM → 전원 · [결정] 4M(Man·Machine·Method·Material) 중 **Material 관리 기준 수립** 착수. 산출물은 구현이 아니라 **계획서**다. 전담 2인을 새로 둔다 — 윤태경(자재 마스터·BOM 표준 설계, `.claude/agents/fw-material-architect.md`), 노하린(실데이터 검증, `.claude/agents/fw-material-validator.md`). 요구사항: (1) 제품자재를 BOM과 BOP에 넣을 수 있는 구조, (2) **모자관계(부모-자식) 필수**, (3) 잘 만들어진 표준·사례를 참조한 **트리 구조** 설계. 진행: 1라운드 윤태경 초안 ∥ 노하린 현행 실측·검증계획 → 2라운드 노하린 프로토타입 실증 검증 → 3라운드 윤태경 반영·확정 → 4라운드 노하린 최종 검증 서명. 문서 위치 `docs/4m/`. 서버 코드·운영 DB는 이번 라운드에서 건드리지 않는다.
- 2026-09-12 09:00 · PM → 윤태경, 노하린 · [알림] 실데이터에서 이미 보이는 자료 품질 문제 두 가지를 반드시 다뤄라. (1) `docs/bldc/bldc-500w-48v.json`의 단품 중 부모가 `HA-3000/EX-5000`로 **한 칸에 두 부모**가 적힌 행이 있다(엑셀 평면 BOM의 한계 — 다대다 모자관계가 필요한 근거). (2) 단위가 EA·kg·g·매·m로 섞여 있다. 또 현재 `parts` 테이블은 `parent_pn` 한 개만 가진 평면 구조이고 서브어셈블리 9종은 L1로 따로 들어가 있다.
- 2026-09-12 11:40 · 윤태경 → 전원, PM · [알림] 4M Material 라운드 1 완료 — `docs/4m/자재관리-기준-계획서.md` **v0.9 초안**(13절). 트리 3축: ① 품목 분류 `mat_class`(자기참조) ② **제품 구조 BOM** `bom_header`+`bom_line`(노드 60·간선 59 = 정확히 트리) ③ 로트 계보 `mat_lot`(자기참조)+`lot_genealogy`. 핵심 테이블 10개 DDL(컬럼별 설명)·무결성 규칙 13개(R-1~R-13, 순환참조·유효일자 겹침 트리거 초안 포함)·코드체계·생애주기·ECO·추적단위(SERIAL 1·LOT 35·NONE 24)·현행 대비 차이 18항목·이행 5단계·화면 계획·로드맵 3단계. 상세는 `docs/team/handoff-윤태경.md`. **서버 코드·DB·다른 문서 미수정, 운영 DB는 열지도 않았고, 커밋 안 했다.**
- 2026-09-12 11:40 · 윤태경 → PM · [답변] 지적한 자료 품질 문제 처리 결과. (1) `HA-3000/EX-5000` **10행 전수 분리 확정** — L1 시트 비고 "네임플레이트/오링/글랜드"와 BOP 투입 공정 분기(EX 3종은 B100·B120, HA 7종은 B50·B60·B80)가 근거다. EX-5000 = NP-5010·OR-5020·CG-5030, HA-3000 = 나머지 7종. (2) 단위는 `uom` 6종 등록(`매`→코드 `SHT`), `uom_conv`에 품목별 환산(SC-1011 1매 = 0.010625 kg = 0.85÷80, xlsx 04·10 시트 대조). `SET`은 EX-5000 팬텀화로 소멸. 소수 자릿수 규칙(EA 0·kg 3·g 2·m 2)에 **현행 49행 전부 통과**. (3) 평면 `parts` → `item` + `bom_header`/`bom_line`으로 전환, 서브어셈블리 9종은 L1 분리가 아니라 BOM 라인의 부모가 된다.
- 2026-09-12 11:40 · 윤태경 → PM · [질문] 초안이 임의로 정할 수 없는 **열린 쟁점 9건**(계획서 §11.3). 판단 필요 4건: (1) `FS-7020`이 자기 자신을 자식으로 갖고 있다(자기참조) — 자식 "Flange Base"를 **`FS-7023`으로 개번** 제안(대안: BP-3020F/R처럼 접미 `FS-7020B`). 품번은 설계 권한이라 승인 전에는 `TMP-` 임시 품번으로만 프로토타입한다. (2) 신규 품목 2종 등록 — `SC-1010P`(OP-A10 산출 "스테이터 편", 지금 P/N이 없어 계보가 끊긴다)·`PK-0010`(OP-B120 투입 "Carton"이 BOM에 아예 없다, 포장재 누락). (3) **동력전달부 체결 공정이 BOP에 없다** — GB-8000·PL-9000은 B85·B87이 만들지만 이후 어느 공정에도 투입되지 않고, FS-7010·FS-7020·FS-7021·FS-7022·OR-7025 5종은 BOP에 아예 등장하지 않는다. 분해도 5~8단계(DRIVE)는 완성품에 붙는 것으로 그려져 있다. **원천 xlsx에 체결 공정이 빠졌는지, 이 4종이 별매 옵션인지** 확인이 필요하다(README "다음 단계 후보"의 "분해도 5·6단계 공정 연결 보완"과 같은 건). (5) `PE-6000`을 팬텀 조립품으로 볼지 실물 조립품으로 볼지 — 별도 재고를 두고 검사할 계획이면 실물이어야 한다. 나머지 5건(4·6·7·8·9)은 계획서 참조.
- 2026-09-12 11:40 · 윤태경 → 노하린 · [요청] 검증 항목 **V-1~V-12**를 계획서 §13에 기대값과 함께 정리했다. 우선순위 최상 4건: **V-1** SQLite 트리거 본문의 `WITH RECURSIVE`(순환 참조 차단 R-1)가 실제로 동작하는지 — `FS-7010 → FS-7010` INSERT가 ABORT 되어야 한다. 동작하지 않으면 적재 계층 검사로 설계를 후퇴시킬 테니 **이것부터 봐 달라**. **V-3** 목표 트리 적재 후 다단계 정전개 합계가 원천 `09_Flat`과 차원별로 맞는지(EA 76·g 221·kg 0.85·매 12·m 0.5에 SC-1011 재배치·팬텀 전개·신규 3품목 반영분) — 차이 나는 항목을 전부 지목해 달라. **V-9** 로트 계보 1건 완주(코일 로트 → 서브로트 → SC-1010P → SC-1010 → SA-1000 → 완성품 시리얼, 정·역방향). **V-12** §9.2 정제 판단 **반박** — 특히 P-1(HA/EX 분리)·P-3(SC-1011 부모를 SC-1010P로 재배치)·P-10(IP-1040→A30, LC-1080→A50 **추정**)·§4.4(팬텀 3종). 내 근거는 BOP 투입 패턴과 L1 시트 비고뿐이다. xlsx 원본이나 분해도 PDF에 다른 증거가 있으면 그쪽이 우선이고, 틀렸으면 v1.0에서 고친다.
- 2026-09-12 11:40 · 윤태경 → 최민준, 서지안, 한도윤 · [알림] 이번 라운드는 계획서만이라 **요청 없음, 영향 없음**. 확인만 남긴다 — 서지안: 자재 모델은 `processes.ct_sec`를 건드리지 않으므로 라인 밸런스 카드에 영향 없다. 한도윤: 분해도 9단계·썸네일 경로 규칙(`/assets/parts/thumb/<pn>.png`) 그대로 유지한다. 최민준: 계획서 §10.3에 **깨뜨리지 않을 계약**을 못 박았다 — `equipment:detail.process.inputs[]`의 키와 의미(`pn, name, spec, qty, unit, parentPn, parentName, image`)는 바꾸지 않고 필드 추가만 한다(`parentPn`은 `HA-3000/EX-5000` → `HA-3000`/`EX-5000`로 **값만** 바뀐다). 이행 3단계에서 `process_inputs`를 호환 뷰로 대체해 `server/index.js` 쿼리 무수정으로 새 데이터를 읽게 할 계획이니, 2단계 시작 전에 이 방식이 가능한지 의견을 달라. 스키마 마이그레이션·콘솔 🧬 자재 탭 훅·`FW_SEED_PRODUCT_LINE=bldc` 자동 시드 확장은 **2단계에 정식 요청**하겠다.
- 2026-09-13 10:40 · 노하린 → 윤태경, PM · [알림] 4M Material **라운드 2(실증 검증) 완료**. 계획서 v0.9 를 문서로 읽지 않고 **실행해서** 깨뜨렸다. 결과는 `docs/4m/검증보고서.md` **§5**(PM 요청서의 "§3 실증 검증 결과"가 이 절이다 — 라운드 1 의 §3·§4 번호를 다른 문서가 참조하고 있어 번호를 바꾸지 않았다). 판정 **확인 99 · 결함 35(정리하면 D-1~D-21) · 의견 1 · 미실시 2**. 내역: DDL 원문 실행 25문(실패 1) · V-1 순환 트리거 **python sqlite3 3.50.4 와 node:sqlite 3.53.0 양쪽** 10항목(결함 4) · BLDC 실데이터 적재 60품목·59라인·BOP 56행(**막힌 행 0건**) · 테스트 케이스 TC-01~59 + 파생 **80건**(PASS 56 · FAIL 22 · BLOCKED 2) · V-12 반박 13항목 · V-9 로트 계보 1건 완주. 프로토타입·실행 로그는 `docs/4m/prototype/v09/`(README 에 실행 순서). **운영 DB `data/factory.db` 는 이번엔 열지도 않았고, 서버 코드·계획서 본문·공용 문서 미수정, 커밋 안 했다.**
- 2026-09-13 10:40 · 노하린 → 윤태경 · [답변] **V-1 답: 동작한다. 설계를 후퇴시킬 필요 없다.** 트리거 본문의 `WITH RECURSIVE` 가 `CREATE` 도 되고 CTE 안 `NEW.child_item_id` 참조도 되고, 10단 전개를 유발하는 INSERT 가 행당 **0.019 ms(python) / 0.043 ms(node)**, 1만 품목·1만 라인 일괄 적재가 **57 ms**다. 적재 계층으로 물러설 이유가 없다. **다만 지금 초안대로 쓰면 순환의 상당 부분을 놓친다 — 결함 4건.** ① **D-3(치명)**: `bom_header.status` 기본값이 `DRAFT` 인데 트리거 CTE 가 `APPROVED/ACTIVE` 만 전개한다 → **DRAFT 사이 순환은 하나도 안 잡힌다. `A→B→A` 가 그냥 들어간다.** 이행 [1] 병행 적재가 바로 그 상태고, 현행 데이터엔 자기참조가 실제로 2건 들어 있다 — **방어가 가장 필요한 순간에 꺼져 있다.** 자기참조(`A→A`)만 잡히는 건 `depth=0` 시드가 `bom_header` 를 안 거치기 때문이라, 네 기대값 `FS-7010→FS-7010` ABORT 는 맞지만 **그 성공이 나머지를 가린다.** 고칠 곳은 한 줄 — `AND h.status <> 'OBSOLETE'`. ② **D-4**: 순환 경로 중간 BOM 이 DRAFT 면 사슬이 끊겨 미검출. ③ **D-1(라운드 1 잔존)**: 순환 길이 **12 이상 통과**(가드 `d.depth < 10`), 메시지는 "레벨 초과"인데 **레벨 초과로는 한 번도 raise 되지 않는다** → 가드 64 + 한계 도달 시 별도 RAISE, "최대 레벨 10"(R-2)은 순환 트리거에서 떼어내 별도 점검 쿼리로 분리할 것. ④ **D-5(치명)**: `bom_line` **UPDATE 트리거가 없다**(초안은 "추가한다"고만 적었다) — `UPDATE bom_line SET child_item_id` 한 번으로 순환을 만들었다. R-9(유효일자)도 같은 문제(D-7). **INSERT 만 막는 규칙은 규칙이 아니다.** 거짓양성은 없었다(깊이 9 사슬·같은 자식 3부모 전부 통과). **V-2 도 확인** — `ux_bom_line_dup` 식 인덱스 정상 동작.
- 2026-09-13 10:40 · 노하린 → 윤태경 · [요청] **다음 라운드에 반드시 고칠 5건.** 이 다섯이 고쳐지면 **서명한다**, 나머지는 v1.1 로 미뤄도 된다. (1) **D-19 — §5.1 `uom_conv` DDL 이 원문 그대로 실행되지 않는다.** `PRIMARY KEY (from_uom, to_uom, COALESCE(item_id,0))` → `expressions prohibited in PRIMARY KEY and UNIQUE constraints`. SQLite 는 **테이블 제약에 식을 못 쓰고 인덱스에는 쓸 수 있다.** python 3.50.4 · node:sqlite 3.53.0 **둘 다** 같은 이유로 거부 — 런타임 문제가 아니라 문법 문제다. 네가 `ux_bom_line_dup` 에서 이미 쓴 방법대로 `CREATE UNIQUE INDEX ux_uom_conv ON uom_conv(from_uom,to_uom,COALESCE(item_id,0))` 로 옮기면 된다. **`PRIMARY KEY (from_uom,to_uom,item_id)` 로 바꾸는 건 안 된다** — SQLite 는 rowid 테이블 PK 열에 NULL 을 허용해 전역 환산이 무제한 중복된다. (2) **D-3** 순환 차단 상태 필터(위 답변). (3) **D-5** UPDATE 트리거 + D-1 깊이 가드. (4) **D-10 — 이행 [3] 호환 뷰가 §10.3 계약을 스스로 깬다.** `process_inputs` 를 뷰로 갈아 끼우니 36행→48행이 되고, 무엇보다 **`qty` 의 의미가 바뀐다** — 뷰는 `bom_line.qty_per`(**부모 base_qty 당**), 기존 컬럼은 **완성품 1대당**이다. 지금 값이 같아 보이는 건 `parentPerProduct` 가 전부 1.0 이라서다 — **네가 P-7 에서 "위험 신호"라고 쓴 그 조건.** 수치로 고정했다(TC-57b): `GB-8000` 을 완성품당 2개로 바꿔도 뷰의 `GB-8010` 은 1.0 그대로다(2 가 나와야 한다). 뷰는 `qty_per` 가 아니라 **전개 계산값**을 내보내야 한다. (5) **D-15 — 로트 계보에 무결성 장치가 하나도 없다.** `lot_genealogy` 자기참조 통과(R-13 이 규칙표엔 "CHECK"인데 §5.6 DDL 에 그 CHECK 가 없다·D-14), 계보 순환 통과(추적 쿼리가 무한 루프한다), `mat_lot.parent_lot_id` 분할 순환 통과, `UNIQUE(out,in,process_id)` 가 `process_id IS NULL` 에서 무력(D-16). 나머지 결함 D-6·D-7·D-8·D-9·D-11·D-12·D-13·D-17·D-18·D-20·D-21 은 §5.6 표에 재현 절차·기대값·제안을 붙였다.
- 2026-09-13 10:40 · 노하린 → 윤태경 · [답변] **V-12(정제 판단 반박) — 확인 4 · 결함 8 · 의견 1.** 원천 xlsx 12시트를 다시 읽고 판정했다. **뒷받침된다(확정해도 된다)**: `P-1` HA/EX 10행 분리(06시트 B2 가 **제목이 아니라 데이터 칸** = `HA-3000` 하나뿐 · 03시트 EX-5000 의 'L2 참조 시트'가 06시트이고 비고가 정확히 3종 지목 · P/N 계열 3xxx↔5xxx · BOP 분기 — 증거 4개가 같은 방향) · `P-2` FS-7010 단품화(03·08 시트 품명이 글자까지 같다) · `P-2` FS-7020 자식 개번(03 "Assy" vs 08 "(Base)" = 다른 물건에 같은 P/N) · `§4.4` HA-3000·EX-5000 팬텀(두 조립품을 산출하는 공정이 24공정 어디에도 없다). **고쳐야 한다**: ① **`FS-7021`·`FS-7022`·`OR-7025` 의 부모가 추정인데 §3.2 트리에 단정해서 그렸다** — 08시트는 부모 4개를 한 칸에 적었고 행별 부모 열이 없다. GB-80xx·PL-90xx 는 BOP(B85·B87)가 확인해 주지만 **FS-70xx 5종은 BOP 에 한 번도 안 나온다.** 특히 `OR-7025` 는 품명이 "O-Ring (**Shaft Seal**)" 수량 2 라 메인 출력축 `FS-7010` 쪽일 수도 있다 → **(추정·확인필요) 표기 + 쟁점 3 포함.** ② **P-3 에 판단이 2개 섞여 있다** — 04시트는 SC-1011 을 SC-1010 바로 밑 L3(Qty/상위 0.85 kg)로 **이미 적고 있어** (가)"부모는 SA-1000 이 아니라 SC-1010"은 원천이 직접 뒷받침한다. 그런데 처리는 (나)"부모를 **SC-1010P** 로"이고 SC-1010P 는 쟁점 2 미승인 신규 품목이다 — **(나)가 부결되면 SC-1011 이 갈 곳이 없다.** 쪼개서 (나)를 쟁점 2 승인에 걸어라. ③ **§4.3 근거 (a) "재고가 선다"는 원천에 없는 서술** — "프레스 산출 후 적층기로 **대차 이동**"이라 썼는데 10시트 OP-A10·A20 **비고 칸이 둘 다 비어 있고** 12시트 어디에도 그런 문장이 없다. **결론(SC-1010P 신설)은 (c) 만으로도 선다. 근거를 부풀리지 마라.** ④ **`uom_conv` SHT→kg 0.010625 는 환산계수가 아니라 수율이다** — 04시트가 주는 건 **코일 소요 0.85 kg** 이지 낱장 무게가 아니다. 블랭킹은 스크랩이 난다고 **네가 쟁점 8 에 썼다.** `scrap_pct` 가 들어오면 `qty_per` 는 `0.85/(1−s)` 로 바뀌고 환산계수는 안 바뀌어야 하는데 같은 숫자를 두 자리에 써 놨다 → **실측 낱장 중량만 넣고, 미측정이면 그 행을 넣지 마라**(낱장 실중량은 원천에 없어 **내가 대체값을 댈 수 없다** — 생산기술 실측 필요). ⑤ **§4.3 기준 (c) 가 배치 공정에서 자기모순** — (c)는 "로트가 **합침**된다"인데 OP-A80(VPI)·OP-A90 은 **30개를 한 배치로 합치면서** "로트는 유지"라며 P/N 을 안 준다. 함침율 ≥95% 불합격이면 **그 30대를 전부** 찾아야 하는데 `proc_state` 텍스트로는 못 묶는다 → **배치 로트 도입** 또는 기준 (c)에서 '합침' 제거. ⑥ **P-10 은 "추정"이 아니라 "원천과 상충"이다** — OP-A30 투입 칸은 `IN-1020U/L, SC-1010` 이라는 **명시적 목록**(IP-1040 없음)이고 OP-A50 은 **`-` 로 "투입 없음"을 선언**했다. **미배정 처리는 옳다.** 근거 문장만 고쳐라. ⑦ **`PE-6000` 이 팬텀(§4.4) + 산출 P/N(§4.3) + 백플러시 지점(§4.5) 을 동시에 받았다** — 팬텀은 로트가 없고 로트가 없으면 백플러시 대상이 아니다. **세 절을 함께 고쳐라.** ⑧ **라운드 1 에 내가 쓴 의견을 철회한다** — 07시트에서 `CN-4030` 은 PE-6000 의 자식인데 BOP 는 그걸 **OP-B100 에서 투입**한다(PE-6000 을 완성하는 **B90 뒤**). 실물 조립품이면 완성 후에 구성품이 못 들어간다 — **이건 팬텀 쪽 증거다.** **쟁점 5 는 'PE-6000 팬텀/실물'로는 못 푼다. 먼저 `CN-4030` 의 부모가 PE-6000 이 맞는지부터 정해라.** 그리고 **신규 규칙 R-22 를 제안한다 — "자식은 부모의 산출 공정보다 늦게 투입될 수 없다"**(`seq(child IN) ≤ seq(parent OUT)`). 점검 쿼리를 돌리니 위반이 정확히 1건 `PE-6000 → CN-4030` 이다. **쟁점 5 논쟁의 실체가 이 한 건이다.** 의견 1건: 04시트 원가 합계 안에 `SC-1010`(8,500)과 그 **원소재** `SC-1011`(2,040)이 둘 다 들어 있다 — `item.std_cost` 주석에 "전개 합산에 쓰지 말 것" 한 줄.
- 2026-09-13 10:40 · 노하린 → PM · [답변] **사용자 3대 요구 충족 판정.** ① **BOM·BOP 에 넣을 수 있는 구조인가 — 조건부 충족.** 실데이터 60품목·59라인·BOP 56행이 **막힌 행 0건**으로 들어갔고 계획서 §5.8 의 행 수 예상(item 60·header 11·line 59·IN 48·OUT 8·팬텀 3·미배정 8)과 **전부 일치**했다. 현행 `parts.parent_pn` 이 표현 못 하던 다대다·다단계·팬텀이 전부 표현된다. 다만 DDL 이 원문대로 실행 안 되고(D-19), 59라인 중 **11 이 공정에 안 붙는데 그게 "팬텀"인지 "미배정"인지 DB 가 구분 못 하고**(D-8), **공정별 소요량을 내는 공식과 쿼리가 계획서에 없고**(D-21), 호환 뷰가 계약을 깬다(D-10). ② **모자관계가 데이터로 강제되는가 — 부분 충족. 아직 "강제된다"고 말할 수 없다.** 반례 **31건**을 실제로 넣어 본 결과 **15건 차단 · 16건 통과**. 뚫리는 것: **DRAFT 구간의 모든 순환(= 적재 중 상태)**, 길이 12 이상 순환, **UPDATE 전 경로 전부**, `valid_to<valid_from`, `bom_header` 리비전 겹침(as-of 가 2건을 반환해 전개 결정 불가), 단위 정합·소수 자릿수(R-7·R-8 이 **규칙만 있고 구현 위치가 비어 있다**), 대체 그룹 동시 유효·우선순위 중복, **로트 계보 무결성 전부**. **D-3 한 줄만 고쳐도 이 판정이 크게 달라진다.** ③ **트리로 성립하는가 — 충족. 이견 없다.** 노드 60 − 간선 59 = **1**(계획서 §3.2 검산과 일치) · 순환 0 · 단일 루트 1 · 고아 0 · 미아 조립품 0 · 도달 불가 0 · 최대 깊이 4 · 다부모 주입 시 역전개가 상위 경로 2개를 전부 낸다. **다만 이 등식은 적재 결과이지 제약이 아니다** — D-3·D-5 때문에 UPDATE 한 번으로 깨져도 DB 가 막지 않는다. "트리로 성립한다"와 "트리로 유지된다"는 다른 말이다. **②를 고쳐야 ③이 지속된다.** 부수 판정: **V-3** 전개 합계는 §1.9 원천과 **차원별 완전 일치**(EA 76·g 221·매 12·kg 0.85·m 0.5)인데 **품목별로는 4건 다르다**(원천에만 `SC-1010`·`FS-7020`, 새 구조에만 `PK-0010`·`FS-7023`, 수량 차이 0건) — **두 쌍이 ±1 EA 로 상쇄돼 합계로는 한 건도 안 잡힌다.** 그래서 **로드맵 2단계 완료기준 ①("차원별 일치")은 통과하지만 검증력이 0이다 → 품목별 대조표로 바꿔야 한다(R-21).** **V-9** 로트 계보는 밀시트 `MS-8842` → 시리얼 `SN-BLDC-2026-000481` **정·역 양방향 완주** — 라운드 1 의 "로트 하나를 역추적할 수 있나? 아니오"가 **'예'로 바뀌었다.** **V-11** 성능은 걱정거리가 아니다(1만 품목 정전개 23.1 ms · 역전개 4.7 ms · 순환검출 0.2 ms · 일괄적재 라인당 0.005 ms). **서명은 계속 보류한다 — D-19·D-3·D-5·D-10·D-15 다섯 건이 고쳐지면 서명한다.**
- 2026-09-13 10:40 · 노하린 → 최민준 · [요청] **이행 [3] `process_inputs` 호환 뷰 방식은 지금 형태로는 성립하지 않는다**(계획서 V-10). `server/index.js` 무수정 전제가 깨진다: 행 수 36→48(정제의 의도된 결과)이고, 무엇보다 **`qty` 의 의미가 바뀐다** — 뷰는 `bom_line.qty_per`(**부모 base_qty 당**), 기존 컬럼은 **완성품 1대당**이다. 지금 값이 같아 보이는 건 현행 `parentPerProduct` 가 전부 1.0 이기 때문이고, 서브어셈블리가 2개 되는 순간 상태창 숫자가 틀린다(TC-57b 로 고정: `GB-8000` 을 2개로 바꿔도 뷰의 `GB-8010` 은 1.0 그대로). 재현은 `cd docs/4m/prototype/v09 && python r2_load.py && python r2_tests.py` → `out/05_tests.log` 의 TC-57·TC-57b. **부탁 2건**: (1) 뷰가 `qty_per` 대신 **전개 계산값**을 내보내는 형태로 가도 되는지 의견을 달라(성능은 문제없다 — 1만 행 전개 23 ms). (2) 확정되면 **운영 DB 의 실제 24공정 투입 단품 표**로 전후 비교가 필요하다 — 나는 이번에도 운영 DB 를 열지 않았고 `prereq_existing.sql` 은 `server/db.js` 에서 옮긴 **스키마 사본**이라 실 데이터 대조는 **미실시**로 남겼다. 참고: `process_material` 의 FK 대상 `processes`·`equipments`·`users`·`production_records` 4개가 없으면 CREATE 는 통과하지만 **INSERT 가 전부 FK 위반으로 죽는다** — 이행 [1] 적재 순서에 못 박아야 한다.
- 2026-09-13 10:40 · 노하린 → 서지안, 한도윤 · [알림] 이번 라운드도 **요청 없음**. 서지안: 라운드 1 에 알린 `process_inputs.qty` 이중 계상(현행 단순합 EA 51 vs 실제 소요 76)은 **새 구조에서 구조적으로 사라진다** — 팬텀 3종(`HA-3000`·`EX-5000`·`PE-6000`)이 IN 행을 갖지 않기 때문이다(TC-26 확인). 다만 자재 소요·원가 지표를 만들 때는 **여전히 BOM 전개값**을 써라. `processes.ct_sec` 는 건드리지 않았고 라인 밸런스 카드에 영향 없다. 한도윤: 분해도 9단계·썸네일 경로 규칙 그대로다. `stage_pn='BP-3020'` 이 실재 품목이 아닌 건 여전하지만(TC-28), 계획서 P-12 가 "분해도는 표시 축이므로 FK 걸지 않는다"고 명시했으니 **네 작업에 영향 없다.**
- 2026-09-13 09:30 · PM → 전원 · [결정] 사용자 지시 "이제 바로 시스템을 만들어라" — Material 기준을 **계획에서 구현으로 전환**한다. 순서: (3라운드) 윤태경이 노하린의 결함 D-1~D-21 중 필수 5건(D-19 DDL 실행 오류·D-3 순환 차단 상태 필터·D-5 UPDATE 트리거·D-10 호환 뷰 수량 의미·D-15 로트 계보 제약)과 V-12 반박 8건을 반영해 **v1.0 확정 + 실행 가능한 DDL 파일 `docs/4m/ddl-v1.sql`**(node:sqlite 3.53에서 그대로 실행되는 것을 노하린 스크립트로 확인) → (4라운드) 최민준이 `server/db.js`에 v1.0 스키마·이행(parts/process_inputs → item/bom_line/process_material, 호환 뷰로 `equipment:detail.process.inputs[]` 계약 유지)·API·BLDC 적재를 구현, 서지안이 관리자 콘솔 **자재 탭**(품목 분류 트리·BOM 트리·where-used·BOM 라인 편집·로트 계보 조회) UI를 `public/js/materials.js`로 병행 → (5라운드) 노하린이 실서버 API로 TC-01~59 인수 검증 → 최민준 통합·push·Render 확인. 게임 화면 계약(설비 클릭 → 단품)은 깨지 않는다.
- 2026-09-13 17:30 · 윤태경 → 전원, PM · [알림] 4M Material **라운드 3 완료 — 계획서 v1.0 확정 + 실행 DDL `docs/4m/ddl-v1.sql` + 적재 규칙 `docs/4m/cleansing-v1.json`.** DDL 은 테이블 12·인덱스 20·트리거 34·뷰 19 = **85문**, 전부 `IF NOT EXISTS`(멱등). 노하린 `r2_ddl.py` 방식(문 단위)으로 **python sqlite3 3.50.4 · node:sqlite 3.53.0 양쪽에서 85문 전부 성공**(단독·선행테이블·2회째). BLDC 적재 검산: item 60·header 11·line 59·IN 46·OUT 24(완성 7)·팬텀 3·미배정 10, 말단 EA 76·g 221·SHT 12·kg 0.85(오차 0)·m 0.5, 반례 72항목 72 OK. 상세 `docs/team/handoff-윤태경.md`. **서버 코드·운영 DB·노하린 프로토타입 미수정, 커밋 안 함.**
- 2026-09-13 17:30 · 윤태경 → 노하린 · [답변] **D-1~D-21 반영표(계획서 §13.1)** — 반영 20 · 해소 확인 1(D-2) · 보류 0. 필수 5건: **D-19** 식 인덱스 `ux_uom_conv(from,to,COALESCE(item_id,0))`(PK 안 씀, 네 근거 채택) · **D-3/D-4** `h.status <> 'OBSOLETE'` · **D-1** 가드 64 + 한계 RAISE, R-2 는 `v_chk_r2_depth` 로 분리 · **D-5** `bom_line` UPDATE OF bom_id,child_item_id + `bom_header` 부모 변경/OBSOLETE 해제 트리거 · **D-7** 겹침 UPDATE 트리거 · **D-10** 호환 뷰 `qty` = 완성품 1대당 전개값(`v_bom_line_qpp`), TC-57b `GB-8000` 2개 → `GB-8010` **2.0** 확인, 뷰가 `rowid` 컬럼을 내서 `ORDER BY i.rowid` 도 산다 · **D-15** 간선 2종(분할+투입)을 비재귀 CTE 로 합친 트리거 4개 + R-13 CHECK(D-14) + `ux_gen_edge` 식 인덱스(D-16). 나머지: D-6 CHECK 두 테이블 · D-8 `bom_line.bop_link`(REQUIRED/PHANTOM/NONE) + `v_bom_line_bop`(ASSIGNED 46·UNASSIGNED 10·PHANTOM 3) · D-9 OUT 24행 `is_final/out_state` + `mat_lot.state_pm_id`(FK, `proc_state` 삭제) · D-11 m→mm 삭제 + `v_uom_base` 환산(MASS 1.071 kg) · D-12 R-9b 승인 BOM 겹침 트리거(DRAFT 허용) · D-13 우선순위 중복 트리거 + 전개 `is_primary` · D-17 `qty_init` 자동·불변 + `v_chk_lot_qty`(차감은 서비스 계층 — 이유 §13.1) · D-18 §4.5 방식별 계보 규칙 + `v_chk_bulk_trace` · D-20 R-7/R-8 트리거 · D-21 §5.9 Q-1~Q-5 + 뷰 7개. **R-18~R-24: R-19 만 보류**(`RAISE()` 는 리터럴만 — 경로는 `v_chk_r1_cycle`), R-22 트리거+뷰(현행 위반 0), R-23 은 `lot_kind='BATCH'` 대신 `proc_batch`/`proc_batch_lot`. 반례: 네가 "뚫린다" 한 16건 전부 거부, 거짓양성 0. **라운드 4 에서 네 하네스로 다시 깨뜨려 달라** — `r2_load.py`·`bomsql.py` 가 바뀔 곳은 handoff 에 적었다(`uom.symbol`, `state_pm_id`, OUT `is_final`, SET 미등록, IN 46/OUT 24, PE-6000 OUT 없음, 곱셈 먼저·나눗셈 마지막).
- 2026-09-13 17:30 · 윤태경 → 노하린 · [답변] **V-12 반박 8건 — 전부 반영.** ① `FS-7021`·`FS-7022`·`OR-7025` 부모 §3.2 `(추정)` + `bom_line.note` + 쟁점 3 ② P-3 을 **P-3a**(부모는 SC-1010 계열 — 04시트 근거) / **P-3b**(SC-1010P — 쟁점 2 조건부, 부결 시 fallback SC-1010 직하) 로 분리 ③ §4.3 (a) "대차 이동" **삭제**, SC-1010P 근거는 (c) 만 ④ `uom_conv` 0.010625 **삭제**, 라인 `qty_basis='GROSS'`(신설 컬럼) — 실측 후 `qty_per`/`scrap_pct` 분리 ⑤ 기준 (c) 에서 '합침' 제거 + `proc_batch` 도입 ⑥ P-10 "추정" → **"원천과 상충"**(A30 명시 목록에 없음·A50 `-`), 미배정 유지 → IN 46 ⑦ PE-6000 §4.3·§4.4·§4.5 동시 수정 ⑧ **PE-6000 팬텀 확정** — 네 CN-4030 근거에 더해 원천 OP-B90 산출 문구가 'PE-6000 완성' 이 아니라 **'제어부 결합'**(실물 4종은 전부 'XX 완성')이고 단독 검사·재고 공정이 없다. CN-4030 부모는 원천(07시트) 유지 — 팬텀이라 어느 쪽이든 전개·화면 동일, R-22 위반 0. 라운드 1 "실물 권장" 철회. 의견 V12-9 `std_cost` 주석 반영, TC-33 SET 미등록(EX-5000 EA), TC-34b 반올림 정책 답 "없음 — 거부".
- 2026-09-13 17:30 · 윤태경 → 최민준 · [요청] **4라운드 구현 계약 (handoff 에 5줄 상세).** (1) `ddl-v1.sql` 을 `server/db.js` 초기화에 **그대로** `exec`(멱등, 선행 테이블 뒤). 고칠 게 있으면 파일 말고 나에게. (2) 이행 [3] 은 DDL 절 H 블록 그대로: `process_inputs`·`parts` → `*_legacy` rename + 같은 이름 뷰(`v_process_inputs_compat`·`v_parts_compat`). `equipment:detail.process.inputs[]` 키·의미 불변, `qty` = 완성품 1대당 전개값, `rowid` 컬럼 있음 → `queries.processInputs`·`listProcesses`·`countParts` **무수정 확인(python·node)**. 단 **뷰 전환 후 `upsertPart`·`addProcessInput`·`deleteProcessInputs`·`POST /api/admin/bop/import`·`apply-sample` 은 실패** → 같은 PR 에서 `cleansing-v1.json` 을 읽는 적재기로 교체. (3) 적재기: `load_order`·`loader_algorithm` 7단계대로. OUT 24 먼저, IN 46 은 `line_process.rows` 순서(pm_id = 상태창 정렬 키). `bop_link` 는 넣지 않는다(트리거). `pn_pending.approved=false` 면 `TMP-` 품번. `item.pn(FG)=products.code=processes.product_code` 등식이 뷰 루트 조건. (4) 적재 후 `expected`·`v_chk_summary`(정상 = {D-8:10, R-10:10}) 대조, 어긋나면 커밋 금지. (5) DELETE 는 트리거가 거부한다(item·bom_header·승인 라인) — 재적재는 UPSERT. 트리거 오류는 `R-1:` `R-9:` `D-8:` 접두 문자열 — API 는 400 본문에 그대로. 노하린 지적대로 `process_material`·`lot_genealogy`·`proc_batch` INSERT 는 `processes`·`equipments`·`users`·`production_records` 가 먼저 있어야 한다. 상태창 값 변화(행 36→46, 구 NULL 2행 값, level 깊이화, `TMP-`)는 계획서 §10.3 표.
- 2026-09-13 17:30 · 윤태경 → 서지안, 한도윤 · [알림] 서지안: 자재 탭 데이터 소스는 전부 뷰·쿼리 — BOM 트리 `v_bom_line_qpp`/Q-1(as-of), where-used Q-3, 공정별 소요 `v_process_requirement`, 미배정 `v_bom_line_bop`, 무결성 `v_chk_summary`+`v_chk_*`, 순환 경로 `v_chk_r1_cycle`(계획서 §10.1). `processes.ct_sec` 무관. 한도윤: 분해도·썸네일 규칙 그대로, `TMP-` 품번 3건만 썸네일이 비어 있다(승인 후 해소).
- 2026-09-13 17:30 · 윤태경 → PM · [질문] **판단이 남은 항목 6건**(계획서 §11.3·handoff 표). ① 쟁점 1 `FS-7023` 개번(대안 `FS-7020B`) ② 쟁점 2 `SC-1010P`·`PK-0010` 신규 — 둘 다 승인 전에는 **`TMP-SC1010P`·`TMP-FS7023`·`TMP-PK0010` 으로 적재**되어 화면에 그렇게 보인다. 승인하면 `cleansing-v1.json` `pn_pending.approved=true` 로 바꾸고 적재기 재실행(`pn` UPDATE 1건, FK 영향 없음). ③ 쟁점 3 체결 공정 BOP 보완(미배정 8건, 생산기술) ④ 쟁점 5 **`PE-6000` 팬텀 확정 — 이의 없으면 이대로**(되돌리는 길 §4.4, 스키마 변경 없음) ⑤ 쟁점 6 `IP-1040`·`LC-1080`(원천이 '투입 없음' — 생산기술) ⑥ 쟁점 7·8 추적 단위·실측(품질·생산기술). 쟁점 4·9 는 닫았다.
- 2026-09-13 12:15 · 최민준 → 전원, PM · [알림] 스프린트 3 **라운드 4 완료 — 자재(Material) v1.0 구현.** `docs/4m/ddl-v1.sql` 을 `server/db.js` 가 파일로 읽어 문 단위 exec(85문, 실패 시 몇 번째 문인지 로그), 이행 [1]→[2]→[3] 자동(기존 DB: legacy 데이터가 있고 `item` 이 비면 `cleansing-v1.json` 적재기 → expected 대조 → `parts`·`process_inputs` → `*_legacy` + 호환 뷰), `server/materials.js` 적재기 + §10 라우트 전부, `apply-sample`·`FW_SEED_PRODUCT_LINE` 이 새 적재기 사용, 콘솔 🧩 자재 탭 훅. **운영 DB 이행 완료**(백업 `fw-backup-20260913-115025` 먼저): item 60·header 11·line 59·IN 46·OUT 24, legacy 56/36행 보존, `v_chk {D-8:10, R-10:10}`. 설비 24대 상태창 단품 전후 diff = 계획서 §9.3 [2] ④ 그대로(전에만 1·후에만 11·공통 35행 수량차 0·순서차 0). 부하 50명 30초 이동 p95 18.5ms·채팅 p95 8.5ms. 상세·응답 예·검산은 `docs/team/handoff-최민준.md`. **커밋만 했고 push 는 라운드 5(노하린 인수) 뒤.**
- 2026-09-13 12:15 · 최민준 → 서지안 · [알림] 자재 API 전부 살아 있다 — 지금 로컬 3000 에서 바로 호출된다. 훅: `admin.html` 탭 `data-tab="materials"`(🧩 자재)·`#tab-materials`·`css/materials.css`·`js/materials.js`, `admin.js` 가 탭 활성화마다 `window.FWMaterials.mount($('tab-materials'), api)` 호출(멱등하게 작성). 파일이 없으면 서버가 빈 JS/CSS 를 200 으로 준다. `api()` 는 4xx 를 `Error(data.error)` 로 throw 하므로 트리거 문자열(`R-1: …`)은 `e.message` 로 받으면 된다. **응답 예(운영 DB)**: `GET /api/admin/materials/summary` → `{"items":60,"classes":24,"bomHeaders":11,"bomLines":59,"lots":0,"processMaterial":{"in":46,"out":24},"itemsByKind":{"CN":6,"FG":1,"PK":1,"PT":40,"RM":2,"SA":10},"phantoms":3,"tmpItems":3,"check":{"D-8":10,"R-10":10},"schema":{"compatViews":true,"legacyTables":false}}` · `GET …/classes` 원소 `{"id":2,"code":"FG-MOT","name":"모터 완제품","parentId":1,"level":1,"itemCount":1,"isLeaf":true,"sortNo":11,"defTraceMode":"SERIAL","defIssueMethod":"BACKFLUSH","shelfLifeDays":null,"note":null}` · `GET …/items` 원소 `{"itemId":50,"pn":"GB-8010","name":"Gearbox Housing","nameKo":null,"spec":"AL 다이캐스트","kind":"PT","classId":13,"classCode":"PT-MCH","className":"기계가공품","uom":"EA","uomSymbol":"EA","status":"ACTIVE","traceKind":"NONE","isTmp":false,"isPhantom":false,"phantom":false,"sourceType":"BUY","image":"GB-8000.png","gtin":null,"drawingNo":null,"stdCost":null,"effFrom":"2026-01-01",…}` · `GET …/items/GB-8010` → `{ item, whereUsed:[{"lineId":49,"parentPn":"GB-8000","parentName":"Gearbox Assy","qtyPer":1,"uom":"EA","headerId":10,"status":"ACTIVE","bopLink":"REQUIRED","effFrom":"2026-01-01","effTo":"9999-12-31"}], headers:[], lots:0, outAt:[], inAt:["OP-B85"] }` · `GET …/bom/BLDC-500W-48V` → `{ root:{pn,name,kind:"FG",uom:"EA",phantom:false,status:"ACTIVE"}, asOf:"2026-09-13", qty:1, nodes:[…], count:59, totals:{"EA":76,"kg":0.85,"g":221,"SHT":12,"m":0.5}, totalsBase:{"COUNT(EA)":76,"MASS(kg)":1.071,"LENGTH(m)":0.5} }` 노드 = `{"lineId":1,"headerId":1,"rev":"A","level":1,"lineNo":10,"parentPn":"BLDC-500W-48V","pn":"SA-1000","name":"Stator (Armature) Assy","kind":"SA","uom":"EA","itemUom":"EA","qtyPer":1,"baseQty":1,"qtyPerParent":1,"qtyPerProduct":1,"qtyPerProductGross":1,"scrapRate":0,"qtyBasis":"NET","phantom":false,"traceKind":"LOT","itemStatus":"ACTIVE","image":"SA-1000.png","altGroup":null,"altPriority":null,"isOptional":false,"bopLink":"REQUIRED","bop":{"op":"OP-B50","mode":"IN","state":"linked","inOps":["OP-B50"],"outOp":"OP-A100"},"effFrom":"2026-01-01","effTo":"9999-12-31","note":null,"children":[…]}` (팬텀 노드는 `phantom:true, bop.state:"phantom"`, 미배정은 `state:"unassigned", op:null`) · `GET …/where-used/SC-1011` → `{"pn":"SC-1011","name":"규소강판 원소재","asOf":"2026-09-13","paths":[[{"pn":"SC-1011","qtyPer":1,"qtyCum":1},{"pn":"TMP-SC1010P","qtyPer":0.010625,"qtyCum":0.010625},{"pn":"SC-1010","qtyPer":80,"qtyCum":0.85},{"pn":"SA-1000",…},{"pn":"BLDC-500W-48V",…}]],"parents":[{"pn":"TMP-SC1010P","qtyPer":0.010625}]}`(각 원소에 `name` 포함) · `GET …/process/OP-B90` → `{ op, process:{id,productCode,op,seq,line,name,kind,output,inputText}, inputs:[{"pmId":57,"seq":1,"lineId":35,"parentPn":"PE-6000","parentName":"Power Electronics Assy","pn":"PC-4010","name":"Main PCB","kind":"PT","traceKind":"LOT","image":"PE-6000.png","qtyPer":1,"baseQty":1,"qtyPerProduct":1,"uom":"EA","uomSymbol":"EA","splitPct":100,"issueMethod":"BACKFLUSH","note":"[CONFIRMED] …","mode":"IN"}×9], outputs:[{"pmId":21,"pn":"BLDC-500W-48V","uom":"EA","isFinal":false,"outState":"제어부 결합","qtyOut":null,"mode":"OUT"}], unassigned:[{"lineId":5,"parentPn":"BLDC-500W-48V","pn":"FS-7010","qtyPerProduct":1,"uom":"EA"}×10] }` · `GET …/lots` 원소 `{"id":2,"lotNo":"LOT-SC1011-260913-A1","pn":"SC-1011","name":"규소강판 원소재","kind":"SUBLOT","parentLotId":1,"parentLotNo":"LOT-SC1011-260913-A","qty":39.15,"qtyInit":40,"uom":"kg","status":"AVAILABLE","statePmId":null,"stateOp":null,"stateText":null,"supplier":null,"supplierLot":null,"madeAt":null,"expireAt":null,"createdAt":"…"}` · `GET …/lots/3/genealogy?dir=back` → `{ lot:{…}, dir:"back", tree:[{"lotId":2,"lotNo":"…","pn":"SC-1011","kind":"SUBLOT","qty":39.15,"uom":"kg","status":"AVAILABLE","depth":1,"edge":{"kind":"CONSUME","qty":0.85,"uom":"kg","op":"OP-A10","equipment":null,"user":"관리자","at":"…","genId":1},"children":[{"lotId":1,…,"edge":{"kind":"SPLIT","qty":40,…},"children":[]}]}], nodes:2 }` · 400 예 `{"error":"R-1: BOM 순환 참조 — 자식의 하위 구조에 부모가 있다 (또는 깊이 64 초과)"}`. **§10 과 다른 점**: ① `items[].kind` 는 DDL `item_type` 코드(FG/SA/PT/RM/CN/PK)이고 팬텀은 `isPhantom`(필터 `kind=PHANTOM|PART|RAW|PKG` 도 받음) ② BOM 노드에 필드가 더 있고 `bop.state` 에 `none`(NONE 라인) 추가, `bop.inOps[]·outOp` ③ where-used 경로는 품목 자신부터 루트까지 ④ `PUT process/:op/links` 는 IN 집합 전체 대체(OUT 항목이 있으면 OUT 도) ⑤ 로트 추가 라우트 `POST lots/:id/consume`(계보 생성)·`PUT lots/:id`·`GET lots/:id`, 헤더 상태는 앞으로만 ⑥ 추가 `GET check`(v_chk_* 상세)·`GET process`(공정 목록 IN/OUT 건수) ⑦ `POST/PUT items` 본문 키는 camelCase(`classId|classCode, kind, uom, traceKind, isPhantom, status, image…`). 전체는 handoff 「API」·「§10 과 달라진 점」. 네 `public/js/materials.js` 가 지금 62행에서 잘려 있어(작성 중) 콘솔에 SyntaxError 가 뜨고 내 안내 화면이 대신 나온다 — 완성되면 그대로 살아난다.
- 2026-09-13 12:15 · 최민준 → 노하린 · [알림] 인수 테스트 접점. **라우트**: `/api/admin/materials/{summary, check, uom, classes, items[?q&classId&status&kind], items/:pn, bom/:pn[?asOf&depth&qty], where-used/:pn[?asOf], bom-headers/:pn, bom-lines/:id, process, process/:op, lots[?pn&q&status], lots/:id, lots/:id/genealogy?dir=fwd|back}` (GET), `POST items · bom-headers · bom-lines · lots · lots/:id/consume · import-sample`, `PUT items/:pn · bom-headers/:id · bom-lines/:id · process/:op/links · lots/:id`, `DELETE bom-lines/:id`. 전부 `X-Auth-Token`(관리자), 트리거 거부 = **400 `{error:"R-1: …"}` 문자열 그대로**(R-1·R-7·R-8·R-9b·R-11·R-13·R-14·R-22 확인, CHECK 는 `CHECK constraint failed: …`). 삭제: `DELETE items/:pn` 405, `bom_header` 삭제 라우트 없음(상태 OBSOLETE), 승인 라인 DELETE 400 R-11, DRAFT 라인 DELETE 200. **격리 실행**(운영 DB 건드리지 말 것): `FW_DATA_DIR=<빈 임시폴더> PORT=3005 FW_SEED_PRODUCT_LINE=bldc FW_JWT_SECRET=<임의 hex> FW_ADMIN_PASSWORD=<임의> node server/index.js` → 빈 DB 에 DDL·뷰 전환·BLDC 적재가 자동으로 되고 로그에 `자재 expected 일치 · v_chk {"D-8":10,"R-10":10}` 가 찍힌다. 토큰은 비밀번호 로그인 대신 `FW_JWT_SECRET` 으로 HS256 `{"uid":<users.id of admin>,"emp":"admin","exp":<epoch>}` 를 서명(`server/auth.js` 형식, base64url) — 내 리허설 하네스가 그 방식이다. 이행 리허설은 백업본(`node scripts/backup.js` 산출 `factory.db`)을 임시 `FW_DATA_DIR` 에 복사해 기동하면 `[db] 이행 [1]…[3]` 로그가 나오고 `parts_legacy`·`process_inputs_legacy` 가 남아 전후 대조가 SQL 로 된다(`process_inputs_legacy JOIN parts_legacy` vs `process_inputs JOIN parts`). **네가 라운드 2 에 요청한 (2) 운영 DB 실제 24공정 전후 대조 결과**: 행 36→46 · 공통 35행 수량차 0 · 순서차 0 · 전에만 `OP-B90:PE-6000` · 후에만 11행(`OP-A20:TMP-SC1010P`, B90 9종, `OP-B120:TMP-PK0010`) · 값 변화는 `level` 1건(SC-1011 L3→L4)·`parentPn` 11건·`parentName` 19건(짧은 라벨 → 부모 `item.name`) · `name/spec/unit/image` 차 0 · "구 NULL 2행" 은 실 DB 에 없었다(전에도 1.0). 소켓 `equipment:detail` 응답 = 뷰 SQL 결과 동일. TC-57b(`GB-8000` 2개 → `GB-8010` 2.0)는 `PUT bom-lines/:id {qtyPer:2}` 후 `GET bom/BLDC-500W-48V` 의 `qtyPerProduct` 또는 상태창으로 볼 수 있다 — ACTIVE 라인이라 UPDATE 는 허용된다(DELETE 만 거부). `import-sample` 은 UPSERT 라 네 편집이 남으면 `expectedMatch:false` 로 보고한다(삭제 안 함); `{"strict":true}` 면 롤백 400.
- 2026-09-13 12:15 · 최민준 → 윤태경 · [답변] 계약 5줄 전부 지켰다 — (1) `ddl-v1.sql` 파일 그대로 exec(85문, 코드에 복사 없음) (2) 절 H 블록 그대로 rename + 뷰, `processInputs`·`listProcesses`·`countParts` 무수정으로 운영 DB 에서 24공정 46행 (3) 적재기 `loader_algorithm` 1~7 순서, OUT 먼저·IN 은 `line_process.rows` 순서, `bop_link` 안 넣음, TMP 3건 (4) expected·`v_chk_summary` 전부 일치(운영·격리·빈 DB 3곳) (5) DELETE 없음, 트리거 문자열 그대로 400. **DDL·cleansing 파일은 한 글자도 안 고쳤다.** 적재기에서 내가 정해야 했던 것(handoff 「적재 규칙에서 내가 정한 것」): ① 현행 `parts.parent_name` 의 짧은 라벨("Stator Assy"·"Housing/External")은 새 구조에 자리가 없어 호환 뷰 `parent_name` = 부모 `item.name` — 상태창 19행의 라벨이 "Stator (Armature) Assy" 등으로 바뀐다(§10.3 표에 없던 값 변화). 라벨 보존이 필요하면 컬럼을 제안해 달라 ② `item.image` = 자기 `<pn>.png` → 없으면 가장 가까운 조상의 파일(현행 `pn→parentPn` 의 다단 일반화) — 덕분에 `SC-1011` 이 전처럼 `SA-1000.png`, `image` 차 0 ③ FG `spec` = 정격 출력/전압/회전수 조합, 서브어셈블리 `spec` 은 같은 P/N 단품 행(FS-7010 DEMOTE 채택, FS-7020 RENUMBER 미채택) ④ note 형식 `bom_header.note`(+`_valid_from_note`)·`bom_line.note`(`원천: …`)·`process_material.note`(`[CONFIRMED] …`) ⑤ `pn_pending[x].rejected: true` 면 `reparent.fallback_to` 사용(P-3b 부결 경로, 파일 형식 확장 — 지금은 안 씀) ⑥ 재적재 시 `item.status`·`bom_header.status/valid_*` 는 덮어쓰지 않음. 계획서 §9.3 [2] ④ 의 "구 NULL 2행" 은 실 DB 에 없었다(전에도 1.0).
- 2026-09-13 12:15 · 최민준 → PM · [알림] `pn_pending` 승인(쟁점 1·2)은 `cleansing-v1.json` `approved: true` 로 바꾼 뒤 콘솔 [🔩 제품 라인 적용] 또는 `POST /api/admin/materials/import-sample` 한 번이면 `UPDATE item SET pn` 1건으로 끝난다(재기동·이행 불필요). 이번 커밋에 `docs/4m/**`(윤태경·노하린 산출물 — DDL 은 런타임이 읽는다)·두 사람 handoff 를 포함했다 — 다른 PC 에서 클론해도 기동 시 스키마가 만들어져야 하기 때문. `public/js/materials.js`(서지안, 작성 중)는 제외.
- 2026-09-13 20:10 · 노하린 → 윤태경, PM · [알림] 4M Material **라운드 4 — v1.0 `ddl-v1.sql` 재검증 완료, 서명한다.** 네 "85문 OK·반례 72항목 OK" 를 믿지 않고 내 하네스(`docs/4m/prototype/v1/`)로 다시 돌렸다: DDL 원문 문 단위 실행 **python sqlite3 3.50.4 · node:sqlite 3.53.0 양쪽 85/85**(단독·선행·2회째, 객체 12·20·34·19, `foreign_keys` 기본 1) · 반례 **133건을 데이터 파일(`cases.json`)로 만들어 두 런타임이 같은 것을 실행 — 133/133 확인, 런타임 차이 0** (R-1 순환 길이 2~70·DRAFT·UPDATE 3경로·헤더 변경·OBSOLETE 되살리기, R-9/R-9b/D-13 UPDATE, R-7/R-8/R-7b/R-14/R-20, D-8/R-22, R-13 계보 순환·분할·중복·길이 70, R-11) · `cleansing-v1.json` 7단계 참조 적재기 `expected` **24/24** · TC-01~59+파생 **104건 PASS 102 · FAIL 0 · 의견 1(TC-28) · 미실시 1(TC-13)** · 말단 **EA 76·g 221·SHT 12·kg 0.85(오차 0)·m 0.5**, MASS 1.071 kg · 1만 품목 성능 정전개 27.6 ms/역전개 4.5 ms/적재 라인당 0.009 ms(트리거 34개). **서명 조건 D-19·D-3·D-5·D-10·D-15 전부 닫힘(§6.4), 라운드 2 에서 뚫린 16건 재현 0.** 상세 `docs/4m/검증보고서.md` **§6**. 운영 DB·서버·포트 3000 미접근, 커밋 안 함.
- 2026-09-13 20:10 · 노하린 → 윤태경 · [요청] **문서 정정 1건 (F-1, 원인은 나다).** 계획서 §9.3 [2]④·§10.3 의 "구 NULL 이던 `SA-1000`·`RA-2000` qty 가 1.0" 은 **사실이 아니다** — 그 NULL 은 내 v0.9 프로토타입 `r2_load.py` 가 `subassemblies` 수량을 안 읽어 생긴 산물이고, 실제 `server/index.js` `importBop` 은 `qtyOf.set(s.pn, s.qty)` = 1.0 을 넣는다(라운드 1 운영 DB 사본 실측 EA 51 도 그 전제). TC-57 을 `importBop` 규칙으로 다시 돌리니 **이행 전후 공통 35행 수량차 0**(전에만 `(B90,PE-6000)` 1 · 후에만 11 · 단위/이름/규격 차 0 · level·parentPn 변화만 예고분). 두 절 문구를 "공통 행 수량차 0" 으로 고쳐 달라. 의견 3건은 DDL 을 고칠 일이 아니다: F-2 `trg_lot_split_cycle_ins` 는 INSERT 에서 발동 조건이 없는 죽은 코드(무해) · F-3 비순환 65간선 사슬이 깊이 한계 메시지로 거부(R-2 10 이라 실무 영향 없음) · F-4 환산 양방향 두 행을 DB 가 안 막음(v1.1 점검 뷰). 관찰: BEFORE 트리거가 CHECK/UNIQUE 보다 먼저라 자기참조·같은시작일 중복은 `R-13:`·`R-9:` 메시지로 거부된다 — API 가 접두를 그대로 400 에 실으니 문제 없음.
- 2026-09-13 20:10 · 노하린 → 최민준 · [알림] **실서버 인수 테스트 도구 준비 완료** — `docs/4m/acceptance/acceptance.mjs`(Node 내장 fetch 만, §10 라우트로 **83건**: 인증 401 · `import-sample` 2회 멱등·`expectedMatch`·`check {D-8:10,R-10:10}` · uom/분류/품목/TMP- 3 · 정전개 `totals` EA 76·g 221·SHT 12·kg 0.85·m 0.5, 노드 59·깊이 4·`bop.state` 46/10/3 · where-used 루트까지 · 24공정 IN 46/OUT 24/완성 7 · **트리거 거부 400+`R-1:` `R-9:` `R-8:` `R-7:` `R-7b:` `R-9b:` `R-11:` `R-14:` `D-13:` `D-8:`** + 롤백 · as-of · TC-14/57b GB-8000 2개→GB-8010 2 · 로트 계보 코일→시리얼 정·역·밀시트·D-17·R-13 · **호환 계약**: `FW_DATA_DIR` 격리 DB 를 읽기 전용으로 열어 `queries.processInputs` SQL 그대로 legacy 36행↔뷰 46행 대조) + `compat-diff.mjs`(전후 스냅샷 대조) + 자체 시험용 mock. mock 으로 **83건 PASS 82·FAIL 0·NOTE 1** 확인. 실행법 `acceptance/README.md`. **1차 실행은 미실시** — 네 "API 올라옴" 알림이 이번 라운드에 없었다(`server/materials.js` 가 생기는 건 봤지만 이행 중 재기동 금지 지시대로 기동 안 함). 알리면 격리 서버(`FW_DATA_DIR` 임시·`PORT=3003`·`FW_SEED_PRODUCT_LINE=bldc`·임시 비밀번호·시크릿)를 내가 띄워 돌리고 §7 에 판정표를 붙인다.
- 2026-09-13 20:10 · 노하린 → 최민준 · [요청] **인수 실행 전 4건.** (1) **전개 API 는 호출 전 `v_chk_summary.'R-1'` 을 봐라** — Q-1 은 순환이 남아 있어도 raise 하지 않고 가드 64 에서 잘린다(TC-16: 245행이 조용히 나온다). 0 이 아니면 409/400 + `v_chk_r1_cycle` 경로. 트리거 뒤엔 못 들어오지만 레거시 이행 시점엔 들어올 수 있다. (2) `db.js` 문 분리기(BEGIN/CASE/END 깊이)가 내 `sqlite3.complete_statement` 와 방식이 달라 기동 로그 "`ddl-v1.sql` N문" 이 **85** 인지 인수 때 본다. (3) **§10 정정 2건**(네 소유): (a) `items[].kind` 열거 `FG/SA/PHANTOM/PART/RAW/PKG` 에 **CN(부자재 6종)** 이 없다 — 구현이 `item_type` 원값을 내면 열거와 어긋나니 하나로 맞추고 서지안에게 알릴 것. (b) **`lot_genealogy` 쓰기 라우트가 §10 에 없다**(구현엔 `POST …/lots/:id/consume`) — 등재해 달라, 없으면 계보 정·역 인수 불가. (4) 협업로그에 **"API 올라옴"** 한 줄.
- 2026-09-13 20:10 · 노하린 → 서지안, 한도윤 · [알림] 서지안: 인수 테스트가 보는 값 = 화면이 보여 줄 값이다 — 요약 타일 정상 `check` 는 **정확히 `{D-8:10, R-10:10}`**(쟁점 3·6 미배정 10건), BOM 트리 `totals` **EA 76·g 221·SHT(매) 12·kg 0.85·m 0.5**, 노드 59·깊이 4·팬텀 3(`bop.state='phantom'`)·미배정 10(`'unassigned'`), `SC-1011` L4 0.85 kg, 승인 전 신규 3건은 **`TMP-SC1010P`·`TMP-FS7023`·`TMP-PK0010`**(`isTmp`, 썸네일 없음), 트리거 오류는 400 `error` 가 `R-1:`·`R-9:`·`D-13:` 등으로 시작(그대로 표시), 순환 경로는 `GET …/check` 의 `v_chk_r1_cycle`. `items[].kind` 의 CN 은 최민준 답을 기다려라. 한도윤: 영향 없음 — 분해도·썸네일 규칙 그대로, `stage_pn='BP-3020'` 미실재는 여전(의견).
- 2026-09-13 21:30 · 서지안 → 전원, PM · [알림] 스프린트 3 **라운드 4(🧩 자재 탭 화면) 완료.** `public/js/materials.js`(`window.FWMaterials.mount`, 멱등) · `public/css/materials.css`(`.fwm-*`, 테마 변수만). 구성: 요약 타일 6 + 검사 규칙 칩 11(클릭 → `GET /check` 상세) · 좌측 분류 트리(접기/펼치기·하위 누계·클릭 필터) · 품목 목록/상세(where-used·산출/투입 공정·역전개 경로·편집) · BOM 트리(정전개, 팬텀 점선·⚠ 미배정·TMP·GROSS·IN/◀ 산출 칩·as-of·깊이·말단 합계+환산) · 라인 편집(POST/PUT/DELETE, 400 트리거 문자열 그대로) · 헤더 상태 전이 · 공정 연결(IN/OUT/미배정, 링크 편집) · 로트 계보(정/역, 투입 기록) · [🔩 샘플 적재]. 검증: 목 29항목 + 정적 하네스(다크·라이트, 1280/980px) + **실 콘솔 3000**(최민준 서버 그대로, 재기동 안 함) — 실 BOM 합계 EA 76·kg 0.85·g 221·SHT 12·m 0.5, 노드 59·팬텀 3·미배정 10·TMP 3, 실 트리거 400 `R-1:`·`R-11:` 그대로 표시, OP-B90 IN 9/OUT 1, D-8 상세 10건, 샘플 적재 expectedMatch. 실 DB 변경 없음(거부된 쓰기 3건 롤백 + 멱등 적재 1회). 상세는 `docs/team/handoff-서지안.md`. 공용 파일 미수정, 커밋 안 함.
- 2026-09-13 21:30 · 서지안 → 최민준 · [요청] 코드 변경 요청은 없다 — 화면은 실제 응답에 맞췄다. (1) 인터페이스 §10 응답 표를 실제대로 갱신해 달라: `GET bom-headers/:pn` 은 배열이 아니라 `{ pn, name, headers[] }`, 노드 `kind` 는 `item_type` + `phantom` 불리언(§10 의 `PHANTOM`/`PART`/`RAW`/`PKG` 표기와 다름), `bop` 에 `inOps[]`·`outOp`, 계보 노드는 `edge: { kind: 'SPLIT'|'CONSUME', qty, uom, op, equipment, user, at }`, where-used 경로 첫 원소는 자기 자신(`qtyCum` 포함), §10 에 없는 라우트 6개(`GET check`·`GET process`·`GET bom-lines/:id`·`GET/PUT lots/:id`·`POST lots/:id/consume`). (2) 주의 1건: `PUT process/:op/links` 는 IN 집합을 통째로 바꾸고 항목에 `splitPct`·`issueMethod`·`note` 가 없으면 **기본값(100/BACKFLUSH/null)으로 덮는다** — 화면은 현재 값을 같이 보내지만, 서버가 "없으면 유지"로 바꾸면 seq 만 보내는 스크립트에서 SP-4040 PICK 이 BACKFLUSH 로 바뀌는 일이 없다(선택). (3) `?mock=1` 이 `admin.html` 에서도 목을 켠다(상단 "목 데이터" 표식) — README 한 줄. README·운영 가이드·`docs/manual.html` 에 🧩 자재 탭 절 요청(4섹션·샘플 적재·검사 위반 타일 색: 0 초록 / 알려진 {D-8:10,R-10:10} 황색 / 그 외 적색). (4) 검증 중 관리자 토큰은 `data/jwt.secret` 로 발급했고(비밀번호 미입력) 서버는 건드리지 않았다.
- 2026-09-13 21:30 · 서지안 → 노하린 · [알림] 인수 테스트 값 = 화면 값이다(네 20:10 목록 그대로 확인). 화면에서 볼 검증 포인트: ① 요약 `검사 위반 20` 황색 + `D-8 10`·`R-10 10` 칩 황색, 나머지 9 규칙 0 초록, 칩 클릭 상세 10건 = `expected.unassigned_lines` ② BOM 트리(BLDC, 기준일 비움) `노드 59`, 말단 합계 `76 EA · 0.85 kg · 221 g · 12 SHT · 0.5 m`, 팬텀 점선 3, ⚠ 미배정 10, TMP 3, `SC-1011 L4 0.85 kg G`, `TMP-SC1010P 80 SHT`, GB-8000/PL-9000 `◀ OP-B85/B87`, 깊이 1 → 10행 ③ 라인 편집에서 자식을 상위 품목으로 바꿔 [수정] → `R-1:` 빨간 줄, ACTIVE 라인 [삭제] → `R-11:`, 단위 kg→EA → `R-7:`, 소수 자릿수 초과 → `R-8:` — 전부 건수 불변 ④ 공정 OP-B90 `IN 9 · OUT 1 · 미배정 10`, OP-A10 OUT `TMP-SC1010P 완성 80 SHT`, 편집 모드에 팬텀 라인 없음 ⑤ 로트: `+ 로트`(SC-1011 850 kg, 밀시트 MS-8842) → SUBLOT → [투입 기록] → 역/정방향 트리로 V-9 를 화면에서 재현 가능. 화면 데이터 소스는 전부 서버 뷰·쿼리이고 클라이언트 계산은 분류 트리 누계 하나뿐이다.
- 2026-09-13 21:30 · 노하린 → 최민준, PM, 윤태경, 서지안 · [알림] **실서버 인수 테스트 1차 실행 — 통과. 84건 · PASS 83 · FAIL 0 · BLOCK 0 · NOTE 1.** 최민준 12:15 알림을 "API 올라옴"으로 보고 **격리 서버**를 내가 띄웠다: 운영 DB·백업본 대신 `acceptance/tools/make-legacy-db.py` 로 **legacy 상태 DB**(스프린트 2 표 4개 — products 1·processes 24·parts 56·process_inputs 36, 자재 표 없음)를 세션 임시 폴더에 만들고 최민준 커밋본을 `FW_DATA_DIR=<임시> PORT=3003 FW_SEED_PRODUCT_LINE=bldc` + 임시 비밀번호·시크릿으로 기동 → 로그에 `ddl-v1.sql **85문**` · `이행 [1] item 60·header 11·line 59·IN 46·OUT 24(완성 7) · [2] expected 일치 · v_chk {D-8:10,R-10:10} · TMP 3` · `이행 [3] *_legacy 보존 + 뷰`. 결과: `import-sample` 2회 멱등·`expectedMatch:true` · `totals` **EA 76·g 221·SHT 12·kg 0.85·m 0.5**(3 ms) · 노드 59·level 4·팬텀 3·`bop.state` 46/10/3 · `SC-1011` L4 정확히 0.85 · where-used 루트까지 4경로 · 24공정 IN 46/OUT 24/완성 7 · **트리거 거부 400 + 문자열 11종(`R-1:` `R-9:` `R-8:` `R-7:` `R-7b:` `R-9b:`×2 `R-11:` `R-14:` `D-13:` `D-8:`) 그대로, 거부 뒤 OP-B50 IN 롤백, links 재PUT 시 pm_id 불변** · as-of 2027 라인 · TC-14/57b GB-8000 2개 → GB-8010 2·GB-8040 4·EA 81 g 246·`process/OP-B85` 2 (되돌림) · 로트 코일 850 kg → 서브로트 0.85(원로트 849.15) → `consume` 4단 → **`genealogy fwd` 시리얼 도달·`back` 밀시트 MS-8842 도달**, 순환 `R-13:`·자기참조·중복 간선 400 · **호환 계약(DB 직접, `queries.processInputs` SQL 그대로)**: `process_inputs`·`parts` 뷰 + `*_legacy` 표, **36→46행, 전에만 (B90,PE-6000), 후에만 11, 공통 35행 qty·unit·name·spec·image 차 0**, level 1·parentPn 11(예고분) · 응답 3 ms 이하. 결과 `docs/4m/acceptance/out/acceptance-2026-09-13T03-15-05.json`·`acceptance-latest.md`·`compat-diff-*.json`·`server-2026-09-13.log`, 판정표 검증보고서 **§7.2**. 격리 서버는 내렸고 포트 3000 은 안 건드렸다. 첫 시도는 **내 픽스처 잘못**(`work_orders` 축약 스텁)으로 기동 실패 — 도구를 고쳐 재시도, 서버 결함 아님.
- 2026-09-13 21:30 · 노하린 → 최민준, 윤태경 · [요청] 인수에서 남은 것은 코드가 아니라 **문서 정정 3건**이다. ① **인터페이스 §10 `items[].kind`** — 실서버는 `item_type` 원값(`FG/SA/PT/RM/CN/PK`) + `phantom` 플래그를 내고 §10 은 `FG/SA/PHANTOM/PART/RAW/PKG`(CN 없음). 구현 쪽으로 문구를 맞춰 달라(최민준, 서지안 매핑 알림) — 라운드 5 에서 NOTE → PASS 로. ② **§10 에 `POST …/lots/:id/consume`(투입 계보) 등재**(최민준) — 이 라우트가 있어서 계보 정·역 인수가 가능했다. ③ **계획서 §10.3 표에 `parentName` 값 변화 추가**(윤태경) — 현행 짧은 라벨("Stator Assy"·"Housing/External")이 부모 `item.name` 으로 바뀌는 19행. 최민준 handoff ① 과 내 격리 diff 가 정확히 19행으로 일치한다. 라벨 보존 컬럼을 둘지는 PM 판단. + 앞서 요청한 F-1("구 NULL 2행" 문구 정정)은 최민준도 실 DB 에서 같은 것을 봤다("전에도 1.0") — 확정.
- 2026-09-13 14:00 · PM → 윤태경, 최민준 · [알림] PM이 직접 정정: 계획서 §9.3[2]④·§10.3의 "구 NULL 2행" 문구를 노하린 F-1대로 정정하고 `parentName` 19행 값 변화를 §10.3 표에 추가했다(윤태경 소유 파일이나 라운드 5 지연을 막기 위해 PM 권한으로 한 줄 수정, 내용 변경 없음). 사용설명서 `docs/manual.html`에 🧩 자재 탭 한 줄 추가. 라운드 5(통합·push·Render)는 최민준.
- 2026-09-13 23:00 · 최민준 → 전원, PM · [알림] 스프린트 3 **라운드 5(통합·인수·배포) 완료.** 세 사람 산출물(서지안 `materials.js`·`materials.css`, 노하린 `docs/4m/acceptance/**`·프로토타입 v1·검증보고서 §6·§7, PM 의 계획서 F-1 정정·`manual.html` 한 줄) + 내 반영분을 한 커밋으로 GitHub `main` 에 push 했다(해시·Render 확인은 `handoff-최민준.md` "라운드 5" 절 + 후속 `[skip render]` 기록 커밋). 반영: 인터페이스 §10 표를 **구현 기준으로 정정**(서지안·노하린 요청 전부), 전개 API **R-1 방어**(노하린 요청), `PUT process/:op/links` **없으면 유지**(서지안 제안 채택), README 🧩 자재 탭 사용법·파일 위치·이행 링크, 운영 가이드 §3-2, design-brief §3·§4 4M Material 문단. 검증: `node --check` 26파일 · 서버 재기동 · 콘솔 🧩 탭 실제 확인(다크·라이트: 타일 `품목 60 · 분류 24 · 헤더 11 · 라인 59`, 검사 위반 20 황색 `D-8 10 · R-10 10`, BOM 노드 59 · 말단 합계 76 EA·0.85 kg·221 g·12 SHT·0.5 m, 라인 추가 폼 자기참조 → 빨간 줄 `R-1: BOM 순환 참조 …` 건수 불변, 로트 계보 역 5노드·정 4노드) · 게임 상태창 OP-A40 `MW-1030 … 180 g` 이행 전과 동일 · **노하린 `acceptance.mjs` 직접 실행** legacy DB 경로 **84건 PASS 83 · FAIL 0 · NOTE 1**, 빈 DB 시드 경로 PASS 81 · FAIL 2(9단계 — legacy 행 없음 전제) · NOTE 1 · 부하 50명 30초 이동 p95 **17.5ms** · 채팅 p95 **13.2ms**. 되돌린 산출물 없음 — 두 사람 다 규격대로였다.
- 2026-09-13 23:00 · 최민준 → 서지안 · [답변] (1) §10 응답 표를 네 "차이" 표대로 갱신했다 — `bom-headers/:pn` 객체, 노드 `kind`=`item_type` 원값 + `phantom`, `bop.inOps[]·outOp`, where-used 첫 원소 자기 자신 + `qtyCum`, 계보 노드 `edge{kind,qty,uom,op,equipment,user,at}`, 라우트 6개(`check`·`process`·`bom-lines/:id`·`GET/PUT lots/:id`·`consume`) 등재, `PUT links` 통째 교체 규칙. (2) **채택** — `PUT process/:op/links` 는 항목에 `splitPct`·`issueMethod`·`note` 가 **없으면(undefined) 같은 lineId 의 기존 값을 유지**한다(순서가 바뀌어 삭제 후 재삽입되는 경로에서도). 새 lineId 는 100/BACKFLUSH/null, `note:''` 는 비우기. OUT 항목도 같은 규칙(`isFinal`·`outState`·`qtyOut`·`note`). 격리 서버에서 OP-B90 에 seq 만 보내 SP-4040 PICK 유지·pmId 불변, 역순 PUT 뒤에도 PICK 유지, 명시하면 덮어씀 확인. 화면은 값을 같이 보내니 바꿀 것 없다. (3) README 스프린트 3 절에 🧩 자재 탭 사용법 3항목(4섹션·샘플 적재·검사 위반 색 규칙)과 `admin.html?mock=1` 한 줄, 운영 가이드 §3-2 추가. `manual.html` 자재 한 줄은 PM 이 14:00 에 넣은 것을 그대로 커밋. (4) 네 두 파일은 규격(§10 훅·`.fwm-*`·테마 변수만)대로라 **그대로 커밋**했다. 실 콘솔 확인 수치는 위 전원 알림. 브라우저 패널이 980px 아래로 안 줄어 860px 1단 전환은 나도 미확인.
- 2026-09-13 23:00 · 최민준 → 노하린 · [답변] 20:10 (1)~(4) · 21:30 ①~③ 전부 처리. **(1)=④ R-1 방어 구현**: `GET bom/:pn`·`where-used/:pn` 이 호출 전 `v_chk_r1_cycle` 을 읽어 요청 품목·결과 노드가 순환 경로에 있으면 **409** `{ error:"R-1: BOM 순환 참조 n건 … (품번들)", warnings:[{rule:'R-1', count, cycles[경로], touched[]}] }`, 순환이 다른 곳에만 있으면 200 + `warnings[]`, `?force=1` 이면 깊이 64 에서 잘린 결과라도 200. 검증은 격리 DB(3003)에 INSERT 트리거를 잠시 끄고 `GB-8000 rev Z(DRAFT) → BLDC` 순환을 주입 → `summary.check {R-1:3}`, `bom/BLDC` **409**, `bom/BLDC?force=1` 200 + warnings, `bom/SA-1000`(순환 밖) 200 + warnings, `where-used/GB-8010`·`SC-1011` 409 → 정리 후 `check {D-8:10,R-10:10}`·warnings 없음·트리거 복구(POST 자기참조 400 R-1). (2) 문 분리기 **85문** — 네 1차·내 이번 격리 두 번 다 `ddl-v1.sql 85문 실행`. (3)=①② §10 정정 완료: `items[].kind` = `item_type` 원값 **FG/SA/PT/RM/CN/PK** + `isPhantom`(별칭 PART/RAW/PKG/PHANTOM 은 필터만 수용), `POST lots/:id/consume` 등재(본문·차감·400 규칙). ③ 계획서 §10.3 `parentName` 19행은 PM 이 14:00 에 넣은 것을 커밋. (4) "API 올라옴" = 12:15 알림이었다 — 네가 그렇게 읽고 1차를 돌린 게 맞다. **내가 `acceptance.mjs` 를 두 번 직접 돌렸다**(둘 다 `out/` 에 남김, `*.json`·`latest.md` 커밋): ⓐ legacy 36행 DB(`tools/make-legacy-db.py`) → 3004: **84건 PASS 83 · FAIL 0 · BLOCK 0 · NOTE 1** ⓑ 빈 DB + `FW_SEED_PRODUCT_LINE=bldc` → 3003: PASS 81 · **FAIL 2 = AT-91·AT-92**(시드 DB 는 `*_legacy` 가 0행이라 36↔46 대조 전제가 없다 — 서버 결함 아님, 도구가 "legacy 0행이면 BLOCK" 으로 판정하면 더 정확하겠다) · NOTE 1. **부탁 2건**: (a) AT-22b 의 `contractKinds` 를 정정된 §10 으로 바꿔 NOTE → PASS(네 파일이라 내가 안 건드렸다). (b) 내 실행이 `out/acceptance-latest.*` 를 덮었다(네 1차 `…T03-15-05` 파일은 그대로). `out/server-*.log` 는 `.gitignore` 의 `*.log` 에 걸려 커밋되지 않았다 — 증거로 남기려면 `.txt` 로 두라.
- 2026-09-13 23:00 · 최민준 → PM · [알림] 계획서 F-1 정정·§10.3 `parentName` 행·`manual.html` 자재 한 줄을 그대로 커밋에 포함했다(내용 무수정). 남은 PM 결정은 그대로: `pn_pending` 승인(쟁점 1·2) → `cleansing-v1.json` `approved: true` → 콘솔 [🔩 샘플 적재], 쟁점 3·6 공정 배정. Render 는 push 후 폴링해 `handoff-최민준.md` 에 적는다(로그인 없이는 시드 결과를 DB 로 확인할 수 없어 `/api/policy` 200·정적 파일·401 만 본다).
- 2026-09-13 14:15 · PM → 윤태경, 최민준, 노하린 · [결정] **신규 품번 3건 승인.** 사용자 승인으로 `SC-1010P`(스테이터 편, SC-1010 밑 80 SHT) · `FS-7023`(Flange Base, FS-7020 밑 1 EA — 자기참조 해소) · `PK-0010`(Carton, 완성품 밑 1 EA)을 정식 품번으로 확정한다. `FS-7020B` 대안은 미채택. 조치: `cleansing-v1.json`의 `pn_pending.*.approved`를 true로 바꾸고(3곳) 운영 DB에 적재기를 1회 실행 — `TMP-` 3건 → 정식 품번, item 60·bom_line 59 불변, `verifyLoad` mismatches 0, `v_chk {D-8:10, R-10:10}` 유지, 호환 뷰 46행에 `SC-1010P@OP-A20 80`·`PK-0010@OP-B120 1` 반영, 뷰의 TMP 0건. 적재 전 백업 `fw-backup-20260913-140301`. 계획서 §11.3 쟁점 1·2를 "닫힘"으로 표기(PM 직접 수정). 남은 PM 판단은 쟁점 3(동력전달부 체결 공정, 생산기술 협의)뿐.
- 2026-09-13 15:40 · PM → 전원 · [알림] **📦 품목 등록 메뉴 신설(스프린트 4).** 사용자 요청: "품목 등록을 별도 메뉴로, 아주 간단하게, 품목구분·사용기한·입고단위 같은 ERP/MES 기준을 넣되 BOM 과 연결되게." 조사 근거는 SAP 자재유형(FERT·HAWA·HALB·ROH·VERP·HIBE)과 국내 ERP 품목구분(제품·상품·반제품·원재료·부재료·저장품) — 공통점은 "구분 하나로 조달·재고·추적 기본값이 정해진다". 그대로 **품목구분 9종**(제품·상품·반제품·재공품·부품·원자재·부자재·소모품·포장재)을 `item.item_group` **표시축**으로 넣었다. 전개·소요량·점검은 종전대로 전부 `item_type` 으로 계산한다 — 계약과 적재기를 건드리지 않으려는 선택이다. `item` 에 NULL 허용 컬럼 4개(`item_group`·`shelf_life_days`·`in_uom`·`in_qty`) 추가: `ddl-v1.sql` 의 `CREATE TABLE item` 안이라 **DDL 문 개수 85 불변**, 기존 DB 는 `server/db.js` 가 `PRAGMA table_info` 로 확인 후 `ALTER TABLE` + 빈 `item_group` 60행 유도 채움. 새 API 2개(`GET item-groups`·`POST bom-attach`)와 화면 `public/js/items.js`·`css/items.css`, 탭 `📦 품목 등록`. 계약은 인터페이스 **§11**. 검증은 운영 DB 사본(격리 3199/3198, 운영 `data/` 미변경): 필수 3칸만으로 등록 → 분류·조달·추적 기본값 자동, 소모품 사용기한 365 상속, 입고단위 `1 CAN = 400 g`, `bom-attach` 로 운영 BOM(RA-2000, ACTIVE)에 오늘자 라인 + `OP-B10` IN 1행 → **D-8·R-10 증가 없음**, 공정 투입 목록에 즉시 반영, 없는 공정이면 400 + 롤백(BOM 라인도 안 남음), R-1 순환·R-22 공정 순서·중복 자식 전부 400. 🧩 자재 탭 회귀 없음(품목 63 = 60 + 시험 3). 노하린 `acceptance.mjs` 75건 PASS 73 · FAIL 2 — **둘 다 내 변경과 무관**하고 오늘 품번 승인으로 `TMP-` 가 사라진 탓이다(AT-22 는 `isTmp 3` 기대, AT-82 는 `TMP-SC1010P` 로 로트 생성). 품목 60·구분별 분포·호환 뷰 대조는 그대로 PASS.
- 2026-09-13 15:40 · PM → 노하린 · [요청] 인수 하네스 기대값 2건을 오늘 승인 결과에 맞춰 갱신해 달라 — **AT-22**(`isTmp 3` → 0, `tmp[]` 빈 배열이 정상) · **AT-82**(`TMP-SC1010P` → `SC-1010P`). 네 파일이라 내가 건드리지 않았다. 겸사겸사 최민준이 남긴 **AT-22b**(`contractKinds` 를 정정된 §10 열거로) 도 같이. 그리고 §11 이 새로 생겼으니 품목 등록 경로(`item-groups`·`group` 필터·`bom-attach`·DRAFT→ACTIVE 승격·`in_uom` 환산 표기)를 다음 라운드 인수 범위에 넣어 달라.
- 2026-09-13 16:20 · PM → 전원 · [알림] **품목 목록을 시트(그리드)로 교체.** 사용자 요청 "품목리스트를 그리드로, 품목코드리스트를 시트 형태로". 좌측 270px 목록을 없애고 **전체 폭 품목 시트 + 아래 등록 폼** 구조로 바꿨다 — 고정 머리글·고정 번호 열·격자선·얼룩 줄·머리글 클릭 정렬(재클릭 역순)·단종 취소선·팬텀 배지·`⬇ 엑셀(CSV)`(UTF-8 BOM). 열 13개(품번·품명·품목구분·분류·규격·기준단위·입고단위·사용기한·추적·조달·쓰이는 곳·자체 BOM·상태)는 `items.js` 의 `COLS` 배열 하나가 화면·정렬·CSV 를 함께 정한다. 시트 값을 채우려고 **목록 API 에만** `usedIn`(유효 `bom_line` 중 자식인 건수) · `hasBom`(자기 헤더 보유)을 더했다 — 상세(`items/:pn`) 응답은 불변이라 §10 계약·자재 탭 영향 없다. 격리 사본에서 확인: 60건, `usedIn` 합계 **59 = bom_line 59**, `hasBom` **11 = bom_header 11**, `IV-1070` 사용기한 `365일*`(분류 상속 표기), 정렬·행 선택·CSV 예외 없음. 계약은 인터페이스 §11.4.
- 2026-09-13 17:05 · PM → 전원 · [알림] **사용기한을 시작일~종료일로 바꾸고 시트를 폭 반응형으로.** 사용자 요청 2건. ① 일수 한 칸(`365일`)이던 사용기한을 **사용기간 시작일·종료일 두 날짜**로 받는다 — 시작일은 기존 `item.eff_from` 을 그대로 쓰고 종료일만 새 컬럼 `item.use_to`(NULL = 무기한). `shelf_life_days` 는 화면에서 빠지고 **두 날짜 차이로 계산해 저장**한다(로트 유효일 = 입고일 + 그 일수). 저장된 종료일이 없으면 `시작일 + 분류 기본 일수`를 계산해 `*` 와 함께 보여 주기만 한다(`useToEffective`/`useToSource`). **판단 기록**: 품목 사용기간(언제까지 쓰는 품번인가)과 로트 보관기한(입고 후 며칠)은 원래 다른 값인데 사용자가 한 칸으로 요청해 합쳤다 — 기간 길이가 곧 보관 일수다. 갈라야 하면 `shelf_life_days` 입력만 다시 열면 된다(컬럼·API 는 그대로 있다). 옛 일수 경로(`shelfLifeDays`)는 인수 하네스·스크립트용으로 살려 뒀다. ② 시트가 폭에 따라 줄어든다 — `COLS[].pri` 1~4 와 CSS `@container fwi` 가 짝이고 1340/1080/820px 에서 차례로 숨는다. 품번·품명·품목구분·상태는 항상 남고 `#`·품번 열은 가로 스크롤에도 고정(620px 이하는 해제). 사용자 스크린샷에서 가로로 밀면 품번이 사라지던 문제가 이걸로 없어진다. 검증(격리 사본): 날짜 등록 365일·수정 121일·지우면 무기한+분류 계산값 복귀·역순 400·형식 400·일수 경로 30일·구분 기본 365일, 폭 700/900/1200/1500px 에서 열 5/8/11/15개 + 가로 넘침 0, 화면에서 종료일 입력 → `545일짜리 기간입니다` 안내 → 저장 → 시트 즉시 반영. 인수 75건 PASS 72 · FAIL 2(전과 같은 `TMP-` 잔재 2건, 새 실패 없음). 계약 §11.2~§11.4 갱신.
- 2026-09-13 18:10 · PM → 전원 · [알림] **🤝 거래처 마스터 신설(스프린트 4).** 사용자 지시: "품목 세부 등록 화면은 별도로 만들어 하위 메뉴에 넣고, 거래처 정보와 연동해야 한다. **먼저 거래처 정보 입력 시스템을 만들어라**(매입·매출·매입/매출·기타)." 지시대로 거래처만 먼저 완결했다. 표는 `server/db.js` 의 **`partners`** — 자재 기준 `ddl-v1.sql` 은 손대지 않았다(문 85개·노하린 인수 기준선 유지). 매입처·매출처를 한 표에 두고 `kind`(BUY/SELL/BOTH/ETC)로 나눈다. **`kind=BUY` 필터에 `BOTH` 가 함께 나오고 칩 건수도 같은 규칙으로 센다** — 목록과 건수가 어긋나면 현장이 못 믿는다. 삭제는 405, 상태(ACTIVE/HOLD/CLOSED)로 관리한다(품목 R-11 과 같은 규칙). 코드를 비우면 `P0001` 부터 빈 번호를 찾아 채운다. **사업자등록번호는 형식 + 국세청 검증식**을 둘 다 보고 `000-00-00000` 으로 정규화, 부분 UNIQUE 인덱스로 중복 차단(빈 값은 여러 건 허용). 화면 `public/js/partners.js` 는 품목 등록과 같은 구조라 **CSS 를 새로 만들지 않고 `items.css` 를 공용**으로 썼다. 검증(운영 DB 사본, 격리 3199): 자동 채번·직접 코드·4개 구분 등록, 검증식 거부/자릿수 거부/중복 거부/이메일 거부/상호 없음/구분 오류 전부 400, 매입 필터 `BOTH` 포함 3건·매출 2건, 코드 변경(PUT) 후 조회, 삭제 405, 화면에서 등록 → 칩 건수·시트 즉시 갱신(`P0002` 빈 자리 재사용 확인). 계약은 인터페이스 **§12**.
- 2026-09-13 18:10 · PM → 최민준 · [예고] 다음은 **품목 세부 등록 화면(하위 메뉴)** 이다. §12.4 대로 `item_partner`(품목 × 거래처 × 역할 + 공급사 품번·단가·리드타임·최소발주량)를 붙이고, 지금 📦 품목 등록 한 칸에 들어 있는 기본 정보와 세부 정보를 나눈다. 메뉴는 상위(자재 기준) 아래 하위 항목으로 접는 구조가 필요하다 — 지금 좌측 탭은 평면이라 `admin.html` 의 `.nav` 를 그룹으로 바꿔야 한다.
- 2026-09-13 19:30 · PM → 전원 · [알림] **품목 세부 화면 + 메뉴 묶음 구조(스프린트 4).** 사용자 승인("이대로 진행")으로 §12.4 예고분을 구현했다. ① **`item_partner`** 표 신설(`server/db.js`, `item`·`partners` 를 둘 다 참조하므로 ddl 실행 뒤에 만든다): 품목 × 거래처 × 역할(BUY/SELL) + 거래처 품번·단가·통화·단가단위·리드타임·최소발주량·발주단위·주거래·유효기간. **주거래처는 품목·역할당 한 곳** — 부분 UNIQUE 인덱스(`WHERE is_primary=1`)로 막고 API 가 새로 켤 때 기존 것을 먼저 내린다. **거래구분과 역할이 어긋나면 400**(매출 전용 거래처를 매입처로 붙이는 실수를 여기서 잡는다, BOTH 는 양쪽 허용). ② 화면 `public/js/itemdetail.js` — 품번 고르기 → 식별정보(도면번호·GTIN·참조단가·이미지) → 매입처·매출처 표(연결·주거래 지정·해제) → BOM 요약(읽기). ③ **좌측 메뉴를 평면 탭 → 운영·기준정보·설정 3묶음**으로. 묶음은 접히고 상태가 `localStorage` 에 남는다. `품목 세부` 는 `품목 등록` 아래 들여쓴 하위 항목이다. ④ **본문 폭 제한 1100px → 시트 탭만 1700px**(`.content.wide`) — 원인 확인: 지금까지 넓은 모니터에서도 시트 열이 접힌 채로만 보인 것이 이 `max-width` 때문이었다(측정값 1044px). 검증(운영 DB 사본, 격리 3199): 매입처 2곳 연결·2차 공급사·BOTH 양쪽·중복 400·역할 오류 400·없는 품목 400·음수 단가 400·기간 역순 400·거래구분 불일치 400·주거래 이동 후 이전 것 자동 해제·거래처 기준 역조회·해제, 화면에서 매출처 연결·식별정보 저장·주거래 지정·메뉴 접기(localStorage) 전부 확인. 인수 75건 PASS 72 · FAIL 2(전과 같은 `TMP-` 잔재 2건, 새 실패 없음). 계약은 인터페이스 **§11.5**.
